"use client";

import { useEffect, useId, useMemo, useState } from "react";

type KeyName = "C" | "G" | "D" | "A" | "E" | "B" | "F#" | "Db" | "Ab" | "Eb" | "Bb" | "F";
type View = "today" | "drills" | "train" | "keys" | "theory" | "chords" | "chord-detail" | "scales" | "scale-library-detail" | "songs" | "song-detail" | "progress" | "routines" | "runner" | "summary" | "grouped" | "drill-detail" | "key-detail" | "scale-detail" | "routine-detail" | "guided";
type Note = { s: number; f: number };

const FIFTHS: KeyName[] = ["C", "G", "D", "A", "E", "B", "F#", "Db", "Ab", "Eb", "Bb", "F"];
const LETTERS = "CDEFGAB";
const LETTER_PC = [0, 2, 4, 5, 7, 9, 11];
const OPEN_PC: Record<number, number> = { 1: 4, 2: 11, 3: 7, 4: 2, 5: 9, 6: 4 };
const STRING_NAMES: Record<number, string> = { 1: "e", 2: "B", 3: "G", 4: "D", 5: "A", 6: "E" };
const PC_KEY: KeyName[] = ["C","Db","D","Eb","E","F","F#","G","Ab","A","Bb","B"];

function keyHue(key: KeyName) { return (289 + FIFTHS.indexOf(key) * 30) % 360; }
function palette(key: KeyName) {
  const h = keyHue(key);
  return { key, h, bright: `oklch(0.58 0.155 ${h})`, ink: `oklch(0.44 0.145 ${h})`, fill: `oklch(0.935 0.032 ${h})`, fill2: `oklch(0.885 0.055 ${h})`, edge: `oklch(0.79 0.07 ${h})`, glow: `oklch(0.9 0.05 ${h})`, dim: `oklch(0.965 0.016 ${h})` };
}
function keyPc(key: KeyName) {
  const li = LETTERS.indexOf(key[0]);
  const accidental = key.slice(1).split("").reduce((sum, mark) => sum + (mark === "#" ? 1 : -1), 0);
  return (LETTER_PC[li] + accidental + 12) % 12;
}
function majorScale(key: KeyName) {
  const li = LETTERS.indexOf(key[0]);
  const root = keyPc(key);
  return [0, 2, 4, 5, 7, 9, 11].map((interval, degree) => {
    const letterIndex = (li + degree) % 7;
    let delta = (root + interval - LETTER_PC[letterIndex] + 12) % 12;
    if (delta > 6) delta -= 12;
    return LETTERS[letterIndex] + (delta > 0 ? "#".repeat(delta) : "b".repeat(-delta));
  });
}
function targetNotes(key: KeyName, low = 1, high = 7) {
  const root = keyPc(key);
  const notes: Note[] = [];
  for (let s = 6; s >= 1; s -= 1) for (let f = low; f <= high; f += 1) if ((OPEN_PC[s] + f) % 12 === root) notes.push({ s, f });
  return notes;
}
function scaleShape(key: KeyName, low: number, high: number) {
  const pcs = [0, 2, 4, 5, 7, 9, 11].map((n) => (keyPc(key) + n) % 12);
  const notes: Note[] = [];
  for (let s = 6; s >= 1; s -= 1) for (let f = low; f <= high; f += 1) if (pcs.includes((OPEN_PC[s] + f) % 12)) notes.push({ s, f });
  return notes;
}
function intervalShape(key: KeyName, intervals: number[], low: number, high: number) {
  const pcs = intervals.map((n) => (keyPc(key) + n) % 12); const notes: Note[] = [];
  for (let s = 6; s >= 1; s -= 1) for (let f = low; f <= high; f += 1) if (pcs.includes((OPEN_PC[s] + f) % 12)) notes.push({ s, f });
  return notes;
}
function chordIntervals(chord: { quality:string; symbol:string }) {
  if (chord.quality === "Minor") return [0,3,7];
  if (chord.quality === "Seventh") return chord.symbol.includes("maj") ? [0,4,7,11] : [0,4,7,10];
  if (chord.quality === "Suspended") return [0,2,7];
  if (chord.quality === "Diminished") return [0,3,6];
  return [0,4,7];
}
function cssVars(key: KeyName) {
  const p = palette(key);
  return { "--key-bright": p.bright, "--key-ink": p.ink, "--key-fill": p.fill, "--key-fill-2": p.fill2, "--key-edge": p.edge, "--key-glow": p.glow, "--key-dim": p.dim } as React.CSSProperties;
}
function playTones(rootKey:KeyName, intervals:number[], sequence=false) {
  if (typeof window === "undefined") return;
  const AudioCtor = window.AudioContext || (window as typeof window & { webkitAudioContext?: typeof AudioContext }).webkitAudioContext;
  if (!AudioCtor) return; const context = new AudioCtor(); const baseMidi = 48 + keyPc(rootKey); const spacing = sequence ? .24 : .035;
  intervals.forEach((interval,index) => { const oscillator=context.createOscillator(); const gain=context.createGain(); const start=context.currentTime+index*spacing; const duration=sequence ? .42 : 1.25; oscillator.type=index%2 ? "sine" : "triangle"; oscillator.frequency.value=440*Math.pow(2,(baseMidi+interval-69)/12); gain.gain.setValueAtTime(.0001,start); gain.gain.exponentialRampToValueAtTime(sequence ? .12 : .075,start+.025); gain.gain.exponentialRampToValueAtTime(.0001,start+duration); oscillator.connect(gain).connect(context.destination); oscillator.start(start); oscillator.stop(start+duration+.03); });
  window.setTimeout(() => void context.close(),(intervals.length*spacing+1.5)*1000);
}
function polar(cx: number, cy: number, r: number, degrees: number) {
  const angle = (degrees - 90) * Math.PI / 180;
  return [cx + r * Math.cos(angle), cy + r * Math.sin(angle)];
}
function wedge(cx: number, cy: number, r0: number, r1: number, a0: number, a1: number) {
  const p1 = polar(cx, cy, r1, a0), p2 = polar(cx, cy, r1, a1), p3 = polar(cx, cy, r0, a1), p4 = polar(cx, cy, r0, a0);
  return `M${p1[0]} ${p1[1]}A${r1} ${r1} 0 0 1 ${p2[0]} ${p2[1]}L${p3[0]} ${p3[1]}A${r0} ${r0} 0 0 0 ${p4[0]} ${p4[1]}Z`;
}

const DRILLS = [
  { id:"position-one",name:"Position one, up and back",category:"Scale fluency",level:"Beginner",minutes:4,progress:72,bpm:84,difficulty:1,low:1,high:5,intervals:[0,2,4,5,7,9,11],reason:"Build one reliable map before connecting the whole neck.",goal:"Two clean round trips",cue:"Keep the turnaround in time." },
  { id:"root-locator",name:"Locate every root",category:"Neck knowledge",level:"Beginner",minutes:3,progress:88,bpm:0,difficulty:1,low:0,high:12,intervals:[0],reason:"Root notes orient every scale, chord and phrase.",goal:"Find all roots in 20 seconds",cue:"Say the string name before each note." },
  { id:"thirds",name:"Thirds through the shape",category:"Intervals",level:"Intermediate",minutes:5,progress:41,bpm:76,difficulty:2,low:1,high:7,intervals:[0,4,7,11],reason:"Thirds reveal chord quality inside a scale shape.",goal:"Three passes without a pause",cue:"Hear each pair before you move." },
  { id:"major-triad",name:"Major triad targets",category:"Chord tones",level:"Beginner",minutes:4,progress:63,bpm:72,difficulty:1,low:0,high:7,intervals:[0,4,7],reason:"The root, third and fifth connect scales directly to chords.",goal:"Resolve every phrase to 1",cue:"Square roots are your landing points." },
  { id:"pentatonic-one",name:"Pentatonic box one",category:"Scale fluency",level:"Beginner",minutes:6,progress:23,bpm:92,difficulty:2,low:3,high:8,intervals:[0,2,4,7,9],reason:"Five-note shapes make phrasing easier while time stays steady.",goal:"Four even eighth-note passes",cue:"Use alternate picking throughout." },
  { id:"minor-pentatonic",name:"Minor pentatonic phrasing",category:"Improvisation",level:"Beginner",minutes:6,progress:54,bpm:78,difficulty:2,low:3,high:8,intervals:[0,3,5,7,10],reason:"A smaller note set lets rhythm and articulation lead.",goal:"Create four two-bar phrases",cue:"Leave one beat of space." },
  { id:"string-six",name:"String six note sprint",category:"Neck knowledge",level:"Beginner",minutes:3,progress:67,bpm:0,difficulty:1,low:0,high:12,intervals:[0,2,4,5,7,9,11],reason:"Low-string roots unlock movable chords and scale positions.",goal:"Name all natural notes twice",cue:"Look away after the first pass." },
  { id:"string-five",name:"String five root map",category:"Neck knowledge",level:"Beginner",minutes:3,progress:48,bpm:0,difficulty:1,low:0,high:12,intervals:[0,5,7],reason:"Most common movable chord roots begin on strings five and six.",goal:"Find 1, 4 and 5 instantly",cue:"Connect each root to a chord shape." },
  { id:"sixths",name:"Sixths on the top strings",category:"Intervals",level:"Intermediate",minutes:5,progress:35,bpm:68,difficulty:2,low:3,high:10,intervals:[0,4,9],reason:"Sixths turn scale knowledge into melodic double-stops.",goal:"One clean ascent and descent",cue:"Let both notes ring together." },
  { id:"sevenths",name:"Leading-tone resolution",category:"Intervals",level:"Intermediate",minutes:4,progress:29,bpm:64,difficulty:2,low:0,high:9,intervals:[0,11],reason:"The seventh-to-root pull makes harmony feel directional.",goal:"Hear ten clean resolutions",cue:"Let 7 lean into 1." },
  { id:"one-four-five",name:"I–IV–V chord-tone map",category:"Chord tones",level:"Intermediate",minutes:6,progress:58,bpm:80,difficulty:2,low:0,high:9,intervals:[0,4,5,7,11],reason:"These three chords power thousands of progressions.",goal:"Track the nearest chord tone",cue:"Move the least distance possible." },
  { id:"relative-minor",name:"Relative minor bridge",category:"Harmony",level:"Intermediate",minutes:5,progress:44,bpm:72,difficulty:2,low:0,high:9,intervals:[0,2,4,7,9],reason:"Major and relative minor share notes but change the tonal center.",goal:"Resolve four phrases both ways",cue:"Change the landing note, not the shape." },
  { id:"arpeggio",name:"Triad arpeggio sequence",category:"Chord tones",level:"Intermediate",minutes:6,progress:31,bpm:76,difficulty:2,low:0,high:12,intervals:[0,4,7],reason:"Arpeggios make harmony audible one note at a time.",goal:"Connect two positions",cue:"Accent the root of each octave." },
  { id:"two-position",name:"Two-position connection",category:"Scale fluency",level:"Intermediate",minutes:7,progress:26,bpm:88,difficulty:2,low:2,high:10,intervals:[0,2,4,5,7,9,11],reason:"Horizontal shifts replace isolated boxes with one neck-wide map.",goal:"Shift without breaking eighth notes",cue:"Change position on string three." },
  { id:"three-note",name:"Three-note-per-string run",category:"Technique",level:"Advanced",minutes:7,progress:18,bpm:96,difficulty:3,low:2,high:10,intervals:[0,2,4,5,7,9,11],reason:"Consistent string groupings improve synchronization and speed.",goal:"Four clean sixteenth-note bars",cue:"Lead each new string with the opposite pick." },
  { id:"diatonic-sevenths",name:"Diatonic seventh arpeggios",category:"Harmony",level:"Advanced",minutes:8,progress:14,bpm:68,difficulty:3,low:0,high:12,intervals:[0,2,4,5,7,9,11],reason:"Seventh chords expose every scale degree’s harmonic role.",goal:"Name all seven chord qualities",cue:"Keep common tones ringing." },
  { id:"mode-shift",name:"Major to Mixolydian",category:"Harmony",level:"Advanced",minutes:6,progress:21,bpm:80,difficulty:3,low:3,high:10,intervals:[0,2,4,5,7,9,10],reason:"Changing one degree shows how modes reshape a familiar key.",goal:"Hear and target the b7",cue:"Compare 7 and b7 directly." },
  { id:"rhythmic-cells",name:"Rhythmic displacement",category:"Technique",level:"Advanced",minutes:6,progress:16,bpm:84,difficulty:3,low:3,high:8,intervals:[0,2,4,7,9],reason:"Moving a five-note idea across four beats builds rhythmic control.",goal:"Stay aligned for eight bars",cue:"Count aloud through the shift." },
] as const;
type Drill = (typeof DRILLS)[number];
function drillNotes(drill:Drill,key:KeyName,full=false) { return intervalShape(key,[...drill.intervals],full ? 0 : drill.low,full ? 12 : drill.high); }
const TRAINING_MODULES = [
  { id:"locator", name:"Note locator", level:"Beginner", minutes:3, intervals:[0], target:"root note", instruction:"Find every root across frets one to seven." },
  { id:"degrees", name:"Scale degrees", level:"Beginner", minutes:4, intervals:[0,4,7], target:"1 · 3 · 5", instruction:"Find the stable major-triad degrees inside the key." },
  { id:"chord-tones", name:"Chord tones", level:"Intermediate", minutes:5, intervals:[0,3,7,10], target:"1 · b3 · 5 · b7", instruction:"Map a minor-seven sound without relying on a shape." },
  { id:"intervals", name:"Interval recall", level:"Advanced", minutes:5, intervals:[0,2,5,9], target:"1 · 2 · 4 · 6", instruction:"Locate the color tones, then name each distance aloud." },
] as const;
const ROUTINES = [
  { name: "Ten minute warm-up", cadence: "Every morning", last: "Yesterday", completed: 3, drills: [{ name:"Open string check",key:"G" as KeyName,mins:4,phase:"warm" },{ name:"Locate every D",key:"D" as KeyName,mins:3,phase:"core" },{ name:"Open chord changes",key:"C" as KeyName,mins:3,phase:"cool" }] },
  { name: "One key, deep", cadence: "Three times a week", last: "2 days ago", completed: 2, drills: [{ name:"Position one, up and back",key:"G" as KeyName,mins:4,phase:"warm" },{ name:"Thirds through the shape",key:"G" as KeyName,mins:5,phase:"core" },{ name:"Chord tones only",key:"G" as KeyName,mins:4,phase:"core" },{ name:"Locate every G",key:"G" as KeyName,mins:3,phase:"cool" },{ name:"Free play over a drone",key:"G" as KeyName,mins:2,phase:"cool" }] },
  { name: "Neck knowledge", cadence: "Weekends", last: "Last Sunday", completed: 1, drills: [{ name:"Pentatonic box one",key:"A" as KeyName,mins:4,phase:"warm" },{ name:"Sixths on the top two",key:"E" as KeyName,mins:3,phase:"core" },{ name:"Open chord changes",key:"C" as KeyName,mins:4,phase:"core" },{ name:"Free play over a drone",key:"F" as KeyName,mins:3,phase:"cool" }] },
];

const CHORDS = [
  { id:"g-major",root:"G" as KeyName,symbol:"G",name:"G major",quality:"Major",formula:"1 · 3 · 5",notes:"G · B · D",fingering:[{s:6,f:3},{s:5,f:2},{s:4,f:0},{s:3,f:0},{s:2,f:0},{s:1,f:3}],tip:"Let the open strings ring; keep the third finger relaxed." },
  { id:"c-major",root:"C" as KeyName,symbol:"C",name:"C major",quality:"Major",formula:"1 · 3 · 5",notes:"C · E · G",fingering:[{s:5,f:3},{s:4,f:2},{s:3,f:0},{s:2,f:1},{s:1,f:0}],tip:"Curve the first finger so the open high E stays clear." },
  { id:"d-major",root:"D" as KeyName,symbol:"D",name:"D major",quality:"Major",formula:"1 · 3 · 5",notes:"D · F# · A",fingering:[{s:4,f:0},{s:3,f:2},{s:2,f:3},{s:1,f:2}],tip:"Start from the open D string and avoid the low E." },
  { id:"a-major",root:"A" as KeyName,symbol:"A",name:"A major",quality:"Major",formula:"1 · 3 · 5",notes:"A · C# · E",fingering:[{s:5,f:0},{s:4,f:2},{s:3,f:2},{s:2,f:2},{s:1,f:0}],tip:"Keep the three second-fret notes compact and let both A notes ring." },
  { id:"e-major",root:"E" as KeyName,symbol:"E",name:"E major",quality:"Major",formula:"1 · 3 · 5",notes:"E · G# · B",fingering:[{s:6,f:0},{s:5,f:2},{s:4,f:2},{s:3,f:1},{s:2,f:0},{s:1,f:0}],tip:"Place the G# first; it is the note that makes the chord major." },
  { id:"f-major",root:"F" as KeyName,symbol:"F",name:"F major",quality:"Major",formula:"1 · 3 · 5",notes:"F · A · C",fingering:[{s:6,f:1},{s:5,f:3},{s:4,f:3},{s:3,f:2},{s:2,f:1},{s:1,f:1}],tip:"Use arm weight instead of squeezing the full first-fret barre." },
  { id:"e-minor",root:"E" as KeyName,symbol:"Em",name:"E minor",quality:"Minor",formula:"1 · b3 · 5",notes:"E · G · B",fingering:[{s:6,f:0},{s:5,f:2},{s:4,f:2},{s:3,f:0},{s:2,f:0},{s:1,f:0}],tip:"Use two adjacent fingers and listen for all six strings." },
  { id:"a-minor",root:"A" as KeyName,symbol:"Am",name:"A minor",quality:"Minor",formula:"1 · b3 · 5",notes:"A · C · E",fingering:[{s:5,f:0},{s:4,f:2},{s:3,f:2},{s:2,f:1},{s:1,f:0}],tip:"Think of C major with fingers two and three shifted inward." },
  { id:"b-minor",root:"B" as KeyName,symbol:"Bm",name:"B minor",quality:"Minor",formula:"1 · b3 · 5",notes:"B · D · F#",fingering:[{s:5,f:2},{s:4,f:4},{s:3,f:4},{s:2,f:3},{s:1,f:2}],tip:"Build the barre first, then place the compact minor shape." },
  { id:"d-seven",root:"D" as KeyName,symbol:"D7",name:"D dominant seven",quality:"Seventh",formula:"1 · 3 · 5 · b7",notes:"D · F# · A · C",fingering:[{s:4,f:0},{s:3,f:2},{s:2,f:1},{s:1,f:2}],tip:"The C on string two creates the pull back toward G." },
  { id:"c-major-seven",root:"C" as KeyName,symbol:"Cmaj7",name:"C major seven",quality:"Seventh",formula:"1 · 3 · 5 · 7",notes:"C · E · G · B",fingering:[{s:5,f:3},{s:4,f:2},{s:3,f:0},{s:2,f:0},{s:1,f:0}],tip:"Lift the first finger from C major to reveal the open B." },
  { id:"g-seven",root:"G" as KeyName,symbol:"G7",name:"G dominant seven",quality:"Seventh",formula:"1 · 3 · 5 · b7",notes:"G · B · D · F",fingering:[{s:6,f:3},{s:5,f:2},{s:4,f:0},{s:3,f:0},{s:2,f:0},{s:1,f:1}],tip:"Keep the first-fret F clear; it creates the pull toward C." },
  { id:"a-seven",root:"A" as KeyName,symbol:"A7",name:"A dominant seven",quality:"Seventh",formula:"1 · 3 · 5 · b7",notes:"A · C# · E · G",fingering:[{s:5,f:0},{s:4,f:2},{s:3,f:0},{s:2,f:2},{s:1,f:0}],tip:"Let the open G ring between the two second-fret notes." },
  { id:"e-seven",root:"E" as KeyName,symbol:"E7",name:"E dominant seven",quality:"Seventh",formula:"1 · 3 · 5 · b7",notes:"E · G# · B · D",fingering:[{s:6,f:0},{s:5,f:2},{s:4,f:0},{s:3,f:1},{s:2,f:0},{s:1,f:0}],tip:"Compare it with E major: lifting one finger reveals the b7." },
  { id:"a-sus-two",root:"A" as KeyName,symbol:"Asus2",name:"A suspended two",quality:"Suspended",formula:"1 · 2 · 5",notes:"A · B · E",fingering:[{s:5,f:0},{s:4,f:2},{s:3,f:2},{s:2,f:0},{s:1,f:0}],tip:"The open B removes the third, leaving an open, unresolved color." },
  { id:"b-diminished",root:"B" as KeyName,symbol:"Bdim",name:"B diminished",quality:"Diminished",formula:"1 · b3 · b5",notes:"B · D · F",fingering:[{s:5,f:2},{s:4,f:3},{s:3,f:4},{s:2,f:3}],tip:"Use the compact symmetry to hear how strongly it wants to resolve to C." },
].map((chord,index) => ({ ...chord, level:index < 8 || chord.quality === "Suspended" ? "Beginner" : index < 15 ? "Intermediate" : "Advanced" }));
const SCALES = [
  { id:"major",name:"Major",family:"Major",level:"Beginner",mood:"clear · resolved",formula:"W W H W W W H",intervals:[0,2,4,5,7,9,11] },
  { id:"natural-minor",name:"Natural minor",family:"Minor",level:"Beginner",mood:"dark · grounded",formula:"W H W W H W W",intervals:[0,2,3,5,7,8,10] },
  { id:"major-pentatonic",name:"Major pentatonic",family:"Pentatonic",level:"Beginner",mood:"open · melodic",formula:"1 2 3 5 6",intervals:[0,2,4,7,9] },
  { id:"minor-pentatonic",name:"Minor pentatonic",family:"Pentatonic",level:"Beginner",mood:"direct · bluesy",formula:"1 b3 4 5 b7",intervals:[0,3,5,7,10] },
  { id:"blues",name:"Blues",family:"Minor",level:"Intermediate",mood:"gritty · expressive",formula:"1 b3 4 b5 5 b7",intervals:[0,3,5,6,7,10] },
  { id:"dorian",name:"Dorian",family:"Modes",level:"Intermediate",mood:"minor · lifted",formula:"1 2 b3 4 5 6 b7",intervals:[0,2,3,5,7,9,10] },
  { id:"mixolydian",name:"Mixolydian",family:"Modes",level:"Intermediate",mood:"major · restless",formula:"1 2 3 4 5 6 b7",intervals:[0,2,4,5,7,9,10] },
  { id:"harmonic-minor",name:"Harmonic minor",family:"Minor",level:"Intermediate",mood:"dramatic · magnetic",formula:"1 2 b3 4 5 b6 7",intervals:[0,2,3,5,7,8,11] },
  { id:"lydian",name:"Lydian",family:"Modes",level:"Advanced",mood:"bright · floating",formula:"1 2 3 #4 5 6 7",intervals:[0,2,4,6,7,9,11] },
  { id:"phrygian",name:"Phrygian",family:"Modes",level:"Advanced",mood:"dark · tense",formula:"1 b2 b3 4 5 b6 b7",intervals:[0,1,3,5,7,8,10] },
  { id:"melodic-minor",name:"Melodic minor",family:"Minor",level:"Advanced",mood:"modern · fluid",formula:"1 2 b3 4 5 6 7",intervals:[0,2,3,5,7,9,11] },
];
const SONGS = [
  { id:"stand-by-me",title:"Stand by Me",artist:"Ben E. King",key:"A" as KeyName,level:"Beginner",capo:"2nd fret",tempo:118,progression:["G","Em","C","D"],focus:"Four-chord pocket",image:"/guitar-stage.jpg" },
  { id:"dreams",title:"Dreams",artist:"Fleetwood Mac",key:"F" as KeyName,level:"Beginner",capo:"None",tempo:120,progression:["F","G"],focus:"Time and consistency",image:"/guitar-strings.jpg" },
  { id:"house-rising-sun",title:"House of the Rising Sun",artist:"Traditional",key:"A" as KeyName,level:"Intermediate",capo:"None",tempo:78,progression:["Am","C","D","F","E"],focus:"Arpeggio control",image:"/guitar-neck.jpg" },
  { id:"three-little-birds",title:"Three Little Birds",artist:"Bob Marley",key:"A" as KeyName,level:"Beginner",capo:"None",tempo:76,progression:["A","D","E"],focus:"Off-beat rhythm",image:"/guitar-stage.jpg" },
  { id:"knockin",title:"Knockin’ on Heaven’s Door",artist:"Bob Dylan",key:"G" as KeyName,level:"Beginner",capo:"None",tempo:70,progression:["G","D","Am","C"],focus:"Smooth changes",image:"/guitar-strings.jpg" },
  { id:"sweet-home",title:"Sweet Home Alabama",artist:"Lynyrd Skynyrd",key:"D" as KeyName,level:"Intermediate",capo:"None",tempo:98,progression:["D","C","G"],focus:"Riff articulation",image:"/guitar-neck.jpg" },
];

function Ring({ value, size = 42, label }: { value: number; size?: number; label?: string }) {
  return <div className="ring" style={{ "--ring-value": `${value * 3.6}deg`, width: size, height: size } as React.CSSProperties} aria-label={label ?? `${value}% complete`}><span>{value}%</span></div>;
}

function Fretboard({ notes, low, high, mini = false, interactive = false, found = new Set<string>(), onCell, labelMode = "note", rootKey = "G" }: { notes: Note[]; low: number; high: number; mini?: boolean; interactive?: boolean; found?: Set<string>; onCell?: (s: number, f: number) => void; labelMode?: "note" | "degree"; rootKey?: KeyName }) {
  const gradientId = `board-${useId().replaceAll(":","")}`;
  const noteMap = new Map(notes.map((note) => [`${note.s}:${note.f}`, note]));
  const unit = mini ? 14 : 44;
  const gap = mini ? 9 : 28;
  const showLabels = !mini;
  const showStringNames = !mini;
  const openWidth = Math.round(unit * .6);
  const padLeft = showStringNames ? Math.round(gap * .7) : 0;
  const frets = Array.from({ length: high - low + 1 }, (_, index) => low + index);
  let cursor = padLeft;
  const columns = frets.map((fret) => {
    const width = fret === 0 ? openWidth : unit;
    const column = { fret, x: cursor, width, center: cursor + width / 2 };
    cursor += width;
    return column;
  });
  const width = cursor;
  const top = mini ? 5 : 15;
  const boardX = low === 0 ? padLeft + openWidth : padLeft;
  const boardHeight = 6 * gap;
  const labelHeight = showLabels ? Math.round(gap * .62) : 0;
  const intervals = ["1", "♭2", "2", "♭3", "3", "4", "♭5", "5", "♭6", "6", "♭7", "7"];
  const noteNames = ["C", "C♯", "D", "D♯", "E", "F", "F♯", "G", "G♯", "A", "A♯", "B"];
  const yFor = (string: number) => top + (string - .5) * gap;
  const dotRadius = Math.round(gap * .4);
  const bottomPad = mini ? 7 : 12;

  return <div className={`fretboard ${mini ? "mini" : ""}`} aria-label={`Guitar fretboard, frets ${low} to ${high}`}>
    <svg viewBox={`0 0 ${width} ${top + boardHeight + labelHeight + bottomPad}`} role="img">
      <defs><linearGradient id={gradientId} x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#303340"/><stop offset=".45" stopColor="#20222d"/><stop offset="1" stopColor="#171923"/></linearGradient></defs>
      <rect x={boardX} y={top - gap * .15} width={width - boardX} height={boardHeight + gap * .3} rx="5" fill={`url(#${gradientId})`} stroke="#3c3f4b" strokeWidth=".8"/>
      {Array.from({length:5},(_,index) => <line key={`grain-${index}`} x1={boardX} y1={top+gap*(index+1)} x2={width} y2={top+gap*(index+1)+1.2} stroke="#85818a" strokeWidth=".35" opacity=".13"/>)}
      {columns.map((column) => column.fret === 0 ? null : <g key={`fret-${column.fret}`}><line x1={column.x} y1={top-gap*.12} x2={column.x} y2={top+boardHeight+gap*.12} stroke={column.fret === 1 && low <= 1 ? "#d7d2c8" : "#777b86"} strokeWidth={column.fret === 1 && low <= 1 ? 3.2 : 1.25}/><line x1={column.x+1.1} y1={top} x2={column.x+1.1} y2={top+boardHeight} stroke="#12141c" strokeWidth=".65" opacity=".7"/></g>) }
      <line x1={width} y1={top-gap*.12} x2={width} y2={top+boardHeight+gap*.12} stroke="#777b86" strokeWidth="1.25"/>
      {columns.filter((column) => [3,5,7,9].includes(column.fret)).map((column) => <circle key={`inlay-${column.fret}`} cx={column.center} cy={top+boardHeight/2} r={mini ? 1.6 : 4.2} fill="#c9c4b9" opacity=".48"/>)}
      {columns.filter((column) => column.fret === 12).flatMap((column) => [2,5].map((string) => <circle key={`inlay-${string}`} cx={column.center} cy={yFor(string)} r={mini ? 1.5 : 3.8} fill="#c9c4b9" opacity=".48"/>))}
      {[1,2,3,4,5,6].map((string) => <g key={`string-${string}`}><line x1={boardX} y1={yFor(string)+.8} x2={width} y2={yFor(string)+.8} stroke="#090a0f" strokeWidth={.7+string*.14} opacity=".72"/><line x1={boardX} y1={yFor(string)} x2={width} y2={yFor(string)} stroke={string > 3 ? "#b7afa0" : "#d9d3c8"} strokeWidth={.55+string*.13} opacity=".9"/></g>) }
      {showStringNames && [1,2,3,4,5,6].map((string) => <text key={`name-${string}`} x={padLeft * .45} y={yFor(string)+gap*.15} textAnchor="middle" fill="#9397ab" fontSize={Math.max(9,gap*.36)} fontWeight="500">{STRING_NAMES[string]}</text>)}
      {[6,5,4,3,2,1].flatMap((string) => columns.map((column) => {
        const id = `${string}:${column.fret}`;
        const hasNote = noteMap.has(id);
        const isFound = !interactive || found.has(id);
        const pc = (OPEN_PC[string] + column.fret) % 12;
        const degree = (pc - keyPc(rootKey) + 12) % 12;
        const label = labelMode === "degree" ? intervals[degree] : noteNames[pc];
        const isRoot = degree === 0;
        const noteAccent = palette(PC_KEY[pc]).bright;
        return <g key={id} onClick={interactive ? () => onCell?.(string,column.fret) : undefined} className={interactive ? "fret-hit" : undefined} role={interactive ? "button" : undefined} tabIndex={interactive ? 0 : undefined} aria-label={interactive ? `${STRING_NAMES[string]} string, fret ${column.fret}${hasNote ? `, ${label}` : ""}` : undefined} onKeyDown={interactive ? (event) => { if (event.key === "Enter" || event.key === " ") onCell?.(string,column.fret); } : undefined}>
          {interactive && <rect x={column.x} y={yFor(string)-gap/2} width={column.width} height={gap} fill="transparent"/>}
          {hasNote && (isRoot ? <rect x={column.center-dotRadius} y={yFor(string)-dotRadius} width={dotRadius*2} height={dotRadius*2} rx={dotRadius*.42} fill={isFound ? noteAccent : "#232532"} stroke={isFound ? "#f8f4eb" : palette(rootKey).edge} strokeWidth={isFound ? 1.2 : 1} strokeDasharray={isFound ? undefined : "3 3"}/> : <circle cx={column.center} cy={yFor(string)} r={dotRadius} fill={isFound ? noteAccent : "#232532"} stroke={isFound ? "rgba(255,255,255,.7)" : palette(rootKey).edge} strokeWidth={isFound ? .8 : 1} strokeDasharray={isFound ? undefined : "3 3"}/>) }
          {hasNote && !mini && <text x={column.center} y={yFor(string)+Math.max(10,dotRadius)*.34} textAnchor="middle" fill={isFound ? "#fffdf8" : palette(rootKey).edge} fontSize={label.length > 1 ? Math.max(8,dotRadius*.82) : Math.max(10,dotRadius)} fontWeight="600" style={{paintOrder:"stroke",stroke:"rgba(20,20,25,.22)",strokeWidth:.7}}>{label}</text>}
        </g>;
      }))}
      {showLabels && columns.map((column) => <text key={`label-${column.fret}`} x={column.center} y={top+boardHeight+labelHeight*.85} textAnchor="middle" fill="#9397ab" fontSize={Math.max(10,Math.round(gap*.42))} fontWeight="500">{column.fret === 0 ? "open" : column.fret}</text>)}
    </svg>
  </div>;
}

function SegmentTabs({ labels, active, onChange }: { labels: string[]; active: string; onChange: (label: string) => void }) {
  return <div className="segment-tabs" role="tablist">{labels.map((label) => <button role="tab" aria-selected={label === active} className={label === active ? "active" : ""} onClick={() => onChange(label)} key={label}>{label}</button>)}</div>;
}

function StatusBar({ end }: { end: string }) { return <div className="status-bar"><span>9:41</span><span>{end}</span></div>; }

function AppHeader({ title, meta, onBack, action }: { title: string; meta?: string; onBack?: () => void; action?: React.ReactNode }) {
  return <><StatusBar end={meta ?? title}/><div className="screen-heading">{onBack && <button className="back" onClick={onBack} aria-label="Go back">←</button>}<h1>{title}</h1>{action}</div></>;
}

function BottomNav({ active, go }: { active: View; go: (view: View) => void }) {
  const items: { label: string; view: View }[] = [{ label:"Today",view:"today" },{ label:"Drills",view:"drills" },{ label:"Train",view:"train" },{ label:"Keys",view:"keys" },{ label:"You",view:"progress" }];
  return <nav className="tab-bar" aria-label="Primary navigation"><div className="desktop-brand" aria-label="FretLab"><span>F</span><div><strong>FretLab</strong><small>Practice one key.<br/>Know the whole neck.</small></div></div><div className="nav-items">{items.map((item,index) => <button key={item.label} onClick={() => go(item.view)} className={item.view === active ? "active" : ""}><i/>{item.label}<small>0{index+1}</small></button>)}</div><div className="desktop-library-nav"><span>Library</span><button onClick={() => go("theory")}>Theory <small>5</small></button><button onClick={() => go("chords")}>Chords <small>{CHORDS.length}</small></button><button onClick={() => go("scales")}>Scales <small>{SCALES.length}</small></button><button onClick={() => go("songs")}>Songs <small>{SONGS.length}</small></button></div><div className="desktop-side-progress"><div><span>Weekly goal</span><strong>68 / 90 min</strong></div><i><b/></i><small>Three sessions this week</small></div><p className="desktop-nav-note">One connected practice system for the entire fretboard.</p></nav>;
}

function DesktopTopbar({ view, selectedKey, go, openKeyPicker }: { view: View; selectedKey: KeyName; go: (view: View) => void; openKeyPicker:()=>void }) {
  const labels: Record<View,string> = { today:"Today",drills:"Drill library",train:"Training modules",keys:"Key explorer",theory:"Theory courses",chords:"Chord library","chord-detail":"Chord study",scales:"Scale library","scale-library-detail":"Scale study",songs:"Song library","song-detail":"Song practice",progress:"Your progress",routines:"Routines",runner:"Session runner",summary:"Session summary",grouped:"Grouped drills","drill-detail":"Drill detail","key-detail":"Key detail","scale-detail":"Scale position","routine-detail":"Routine detail",guided:"Guided routine" };
  return <header className="desktop-topbar"><div><span>Workspace</span><strong>{labels[view]}</strong></div><div className="desktop-topbar-center"><i/><span>Global practice key</span><b>{selectedKey} major</b></div><div className="desktop-topbar-actions"><button className="global-key-button" onClick={openKeyPicker}><span className="topbar-key">{selectedKey}</span><span><small>Global key</small><strong>{selectedKey} major · Circle of fifths</strong></span><b>⌄</b></button><button className="topbar-primary" onClick={() => go("train")}>Quick train <span>→</span></button></div></header>;
}

function DesktopPracticeStudio({ go, sessionKey }: { go: (view: View) => void; sessionKey: KeyName }) {
  const [mode,setMode] = useState<"Notes" | "Degrees" | "Roots">("Roots");
  const [bpm,setBpm] = useState(84);
  const [running,setRunning] = useState(false);
  const [meter,setMeter] = useState<3 | 4>(4);
  const [beat,setBeat] = useState(0);
  useEffect(() => { if (!running) { setBeat(0); return; } const timer = window.setInterval(() => setBeat((value) => (value+1)%meter),60000/bpm); return () => window.clearInterval(timer); },[running,bpm,meter]);
  const notes = mode === "Roots" ? targetNotes(sessionKey,0,12) : scaleShape(sessionKey,0,12);
  const guide = mode === "Roots" ? {title:`Find every ${sessionKey}`,copy:"Squares mark the tonal center. Say the string, then play each root from low E to high e.",target:`Goal · find all ${notes.length} roots in 20 seconds`} : mode === "Degrees" ? {title:"Understand each note’s job",copy:"Numbers show musical function: 1 is home, 3 sets major color, 5 is the stable anchor.",target:"Goal · play 1–3–5 in three different areas"} : {title:"Learn the actual pitch names",copy:"Colors identify pitch consistently; labels tell you exactly which note sits under each finger.",target:"Goal · name each note before you play it"};
  return <section className="desktop-studio" aria-label="Practice studio"><div className="studio-board"><div className="studio-heading"><div><p className="kicker">Practice studio · {sessionKey} major</p><h2>{guide.title}</h2><p>{guide.copy}</p></div><div className="studio-modes" role="group" aria-label="Choose practice studio lesson">{(["Roots","Degrees","Notes"] as const).map((item) => <button className={mode === item ? "active" : ""} onClick={() => setMode(item)} key={item}>{item === "Roots" ? "Find roots" : item === "Degrees" ? "See function" : "Name notes"}</button>)}</div></div><div className="studio-task-strip"><span>1 · Choose a view</span><i>→</i><span>2 · Follow the board</span><i>→</i><strong>{guide.target}</strong></div><Fretboard notes={notes} low={0} high={12} labelMode={mode === "Degrees" ? "degree" : "note"} rootKey={sessionKey}/><div className="studio-legend"><span><i className="root-dot"/>Square = {sessionKey} root</span><span><i/>Circle = another scale tone</span><button onClick={() => playTones(sessionKey,mode === "Roots" ? [0,12] : [0,2,4,5,7,9,11],true)}>▶ Hear this view</button></div></div><aside className="tempo-widget"><div className="tempo-widget-head"><div><span>Step 3 · add time</span><strong>{meter}/4 meter</strong></div><div>{([3,4] as const).map((value) => <button className={meter === value ? "active" : ""} onClick={() => { setMeter(value); setBeat(0); }} key={value}>{value}</button>)}</div></div><div className="tempo-orbit"><div className="beat-orbit">{Array.from({length:meter},(_,index) => <i className={running && beat === index ? "active" : ""} key={index}/>)}</div><strong aria-live="polite">{bpm}</strong><span>bpm</span></div><div className="beat-steps" aria-label={`Beat ${beat+1} of ${meter}`}>{Array.from({length:meter},(_,index) => <i className={running && beat === index ? "active" : ""} key={index}/>)}</div><p className="tempo-help">Start only after you can complete the goal slowly. One note per click.</p><input className="tempo-slider" type="range" min="40" max="180" step="1" value={bpm} onChange={(event) => setBpm(Number(event.target.value))} aria-label="Tempo"/><div className="tempo-controls"><button onClick={() => setBpm((value) => Math.max(40,value-4))} aria-label="Decrease tempo">−4</button><button className="tempo-play" onClick={() => setRunning(!running)}>{running ? "Pause" : "Start click"}</button><button onClick={() => setBpm((value) => Math.min(180,value+4))} aria-label="Increase tempo">+4</button></div><button className="studio-train" onClick={() => go("train")}>Test recall without labels <span>→</span></button></aside></section>;
}

function Today({ go, sessionKey }: { go: (view: View) => void; sessionKey: KeyName }) {
  const week = [["M",14],["T",22],["W",8],["T",18],["F",6],["S",0],["S",0]] as const;
  return <div className="screen-content today-screen"><StatusBar end="31 day streak"/><div className="today-greeting"><p>Thursday</p><h1>Good evening</h1></div>
    <article className="session-hero"><div className="key-watermark">{sessionKey}</div><p className="kicker">Today&apos;s session</p><h2>The key of {sessionKey}</h2><p>Four drills that all live in one key, so the shapes start rhyming.</p><div className="session-stats"><div><strong>18</strong><span>minutes</span></div><div><strong>4</strong><span>drills</span></div><div><strong>84</strong><span>bpm</span></div></div><button className="primary-action" onClick={() => go("runner")}>Start session <span>→</span></button></article>
    <section className="section"><div className="section-head"><h2>This week</h2><span>68 min</span></div><div className="week-chart">{week.map(([day,mins],i) => <div className="day" key={`${day}${i}`}><div className={`bar-track ${i === 4 ? "today" : ""}`} title={`${mins} minutes`}><span style={{ height: mins ? `${Math.max(18,Math.round(mins/22*100))}%` : 0 }}/></div><span>{day}</span></div>)}</div></section>
    <section className="section"><div className="section-head"><h2>Pick up again · {sessionKey} major</h2></div><div className="resume-grid"><button style={cssVars(sessionKey)} onClick={() => go("drill-detail")}><span className="key-chip">Key {sessionKey}</span><Ring value={62}/><strong>Locate every root</strong><small>3 min left</small></button><button style={cssVars(sessionKey)} onClick={() => go("drill-detail")}><span className="key-chip">Key {sessionKey}</span><Ring value={23}/><strong>Pentatonic box one</strong><small>6 min left</small></button></div></section><DesktopPracticeStudio go={go} sessionKey={sessionKey}/>
  </div>;
}

function Drills({ go, selectedKey, onOpen }: { go: (view: View) => void; selectedKey:KeyName; onOpen:(id:string)=>void }) {
  const [filter,setFilter] = useState("All"); const [level,setLevel] = useState("All levels");
  const [query,setQuery] = useState("");
  const visible = DRILLS.filter((drill) => (filter === "All" || drill.category === filter) && (level === "All levels" || drill.level === level) && `${drill.name} ${drill.reason}`.toLowerCase().includes(query.toLowerCase()));
  return <div className="screen-content drill-library-screen"><AppHeader title={`${selectedKey} drill library`} meta={`${visible.length} drills`} action={<button className="text-action" onClick={() => go("grouped")}>Learning paths</button>}/><section className="drill-library-hero"><div><p className="kicker">One key · complete practice system</p><h2>Build the neck in layers.</h2><p>Every drill below is transposed to {selectedKey}. Start with landmarks, connect scales to chord tones, then add time and technique.</p></div><button onClick={() => go("routines")}><span>Guided routine</span><strong>18-minute {selectedKey} habit builder</strong><small>Warm-up → map → harmony → music</small><b>Start path →</b></button></section><div className="drill-filter-stack"><SegmentTabs labels={["All","Neck knowledge","Scale fluency","Chord tones","Intervals","Harmony","Technique"]} active={filter} onChange={setFilter}/><div className="chip-scroll">{["All levels","Beginner","Intermediate","Advanced"].map((item) => <button className={level === item ? "active" : ""} onClick={() => setLevel(item)} key={item}>{item}</button>)}</div></div><div className="desktop-drill-tools"><label><span>Search by skill or outcome</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Try roots, chord tones, rhythm…"/></label><div><strong>{visible.length}</strong><span>{selectedKey} major drills</span></div></div><div className="drill-list">{visible.map((drill,index) => <button className="drill-card expanded" key={drill.id} onClick={() => onOpen(drill.id)} style={cssVars(FIFTHS[(FIFTHS.indexOf(selectedKey)+index)%12])}><Fretboard notes={drillNotes(drill,selectedKey)} low={drill.low} high={drill.high} mini rootKey={selectedKey}/><div className="drill-copy"><div className="drill-card-tags"><span>{drill.category}</span><span>{drill.level}</span></div><strong>{drill.name}</strong><p>{drill.reason}</p><div><span className="key-chip">Key {selectedKey}</span><small>{drill.minutes} min</small><small>{drill.bpm ? `${drill.bpm} bpm` : "untimed"}</small><i>{[1,2,3].map((n) => <b className={n <= drill.difficulty ? "on" : ""} key={n}/>)}</i></div></div><div className="drill-go"><Ring value={drill.progress} size={44}/><span>Open →</span></div></button>)}</div></div>;
}

function Train({ selectedKey, setSelectedKey }: { selectedKey: KeyName; setSelectedKey: (key: KeyName) => void }) {
  const [moduleId,setModuleId] = useState<(typeof TRAINING_MODULES)[number]["id"]>("locator");
  const moduleIndex = TRAINING_MODULES.findIndex((item) => item.id === moduleId); const module = TRAINING_MODULES[moduleIndex];
  const notes = useMemo(() => intervalShape(selectedKey,[...module.intervals],1,7),[selectedKey,module]);
  const [found,setFound] = useState<Set<string>>(() => new Set(notes.slice(0,-1).map((note) => `${note.s}:${note.f}`)));
  const [misses,setMisses] = useState(0);
  useEffect(() => { setFound(new Set(notes.slice(0,-1).map((note) => `${note.s}:${note.f}`))); setMisses(0); },[notes]);
  const hit = (s:number,f:number) => { const id = `${s}:${f}`; if (notes.some((note) => note.s === s && note.f === f)) setFound((current) => new Set(current).add(id)); else setMisses((n) => n + 1); };
  const next = () => { if (moduleIndex === TRAINING_MODULES.length-1) setSelectedKey(FIFTHS[(FIFTHS.indexOf(selectedKey)+1)%FIFTHS.length]); setModuleId(TRAINING_MODULES[(moduleIndex+1)%TRAINING_MODULES.length].id); setMisses(0); };
  const accuracy = Math.round((found.size / Math.max(1,found.size+misses))*100);
  return <div className="screen-content train-screen"><StatusBar end="Training modules"/><section className="train-module-picker"><div className="section-head"><h2>Your module path</h2><span>{moduleIndex+1} of {TRAINING_MODULES.length}</span></div><div>{TRAINING_MODULES.map((item,index) => <button className={item.id === moduleId ? "active" : ""} onClick={() => setModuleId(item.id)} key={item.id}><span>0{index+1}</span><strong>{item.name}</strong><small>{item.level} · {item.minutes} min</small><i><b style={{width:`${item.id === moduleId ? Math.round(found.size/Math.max(1,notes.length)*100) : index < moduleIndex ? 100 : 12}%`}}/></i></button>)}</div></section><div className="train-hero"><p className="kicker">{module.name}</p><strong>{selectedKey}</strong><span>{module.instruction}</span><div><span>{module.target}</span><button onClick={() => playTones(selectedKey,[...module.intervals],true)}>▶ Hear target</button></div></div><div className="train-board"><div className="section-head"><h2>Tap each target</h2><span>Every note has a consistent color</span></div><Fretboard notes={notes} low={1} high={7} interactive found={found} onCell={hit} rootKey={selectedKey}/></div><div className="stat-grid"><article><strong>{found.size}/{notes.length}</strong><span>found</span></article><article><strong>0:24</strong><span>elapsed</span></article><article><strong>{accuracy || 0}%</strong><span>accuracy</span></article></div><button className={found.size === notes.length ? "primary-action" : "secondary-action"} onClick={next}>{found.size === notes.length ? "Complete module" : "Move to next module"} <span>→</span></button><aside className="desktop-train-guide"><div><p className="kicker">Live target map</p><strong>{notes.length-found.size}</strong><span>locations remain</span></div><div className="target-lanes">{[6,5,4,3,2,1].map((string) => { const total = notes.filter((note) => note.s === string).length; const complete = notes.filter((note) => note.s === string && found.has(`${note.s}:${note.f}`)).length; return <div key={string}><span>{STRING_NAMES[string]}</span><i><b style={{width:`${total ? complete/total*100 : 100}%`}}/></i><small>{complete}/{total}</small></div>; })}</div><p>Work string by string. Say each pitch or degree before you tap it, then repeat without fret numbers.</p></aside></div>;
}

function CircleOfFifths({ selectedKey, onSelect }: { selectedKey: KeyName; onSelect: (key: KeyName) => void }) {
  const selected = FIFTHS.indexOf(selectedKey);
  return <div className="key-wheel"><svg viewBox="0 0 316 316" role="img" aria-label="Interactive circle of fifths">{FIFTHS.map((key,index) => { const distance = (index-selected+12)%12; const near = distance === 0 || distance === 1 || distance === 11; const h = keyHue(key); return <g key={key} role="button" tabIndex={0} aria-label={`Select ${key} major`} onClick={() => onSelect(key)} onKeyDown={(event) => { if(event.key === "Enter" || event.key === " ") onSelect(key); }}><path d={wedge(158,158,106,150,index*30-14.4,index*30+14.4)} fill={distance === 0 ? `oklch(.62 .16 ${h})` : near ? `oklch(.885 .06 ${h})` : `oklch(.955 .014 ${h})`}/><path d={wedge(158,158,64,102,index*30-14.4,index*30+14.4)} fill={near ? `oklch(.935 .032 ${h})` : `oklch(.975 .008 ${h})`}/></g>; })}<circle cx="158" cy="158" r="58" fill="var(--key-fill-2)" stroke="var(--key-edge)"/></svg>{FIFTHS.map((key,index) => { const outer = polar(158,158,128,index*30); const inner = polar(158,158,84,index*30); return <div key={key}><button onClick={() => onSelect(key)} className={`wheel-label ${selectedKey === key ? "selected" : ""}`} style={{ left:`${outer[0]/316*100}%`,top:`${outer[1]/316*100}%` }}>{key}</button><span className="minor-label" style={{ left:`${inner[0]/316*100}%`,top:`${inner[1]/316*100}%` }}>{majorScale(key)[5]}m</span></div>; })}<div className="wheel-core"><strong>{selectedKey}</strong><span>major</span></div></div>;
}

function KeySelectorModal({ selectedKey, onSelect, onClose }: { selectedKey:KeyName; onSelect:(key:KeyName)=>void; onClose:()=>void }) {
  return <div className="key-modal-backdrop" role="presentation" onMouseDown={onClose}><section className="key-modal" role="dialog" aria-modal="true" aria-labelledby="key-modal-title" onMouseDown={(event) => event.stopPropagation()}><header><div><p className="kicker">Global key context</p><h2 id="key-modal-title">Choose one key for the whole workspace</h2><p>Drills, theory, scales, chords and routines will immediately transpose together.</p></div><button onClick={onClose} aria-label="Close key selector">×</button></header><div className="key-modal-layout"><CircleOfFifths selectedKey={selectedKey} onSelect={(key) => { onSelect(key); onClose(); }}/><div className="key-modal-summary" style={cssVars(selectedKey)}><span>Active everywhere</span><strong>{selectedKey} major</strong><p>{majorScale(selectedKey).join(" · ")}</p><div>{majorScale(selectedKey).map((note,index) => <i key={note} style={{background:palette(PC_KEY[(keyPc(selectedKey)+[0,2,4,5,7,9,11][index])%12]).bright}} title={note}/>)}</div><small>Changing the key preserves the lesson and moves every pitch relationship.</small></div></div></section></div>;
}

function LibraryLaunchpad({ go }: { go: (view: View) => void }) {
  const cards: {view:View;label:string;copy:string;image:string;count:string}[] = [{view:"theory",label:"Theory courses",copy:"Intervals, harmony and rhythm you can see.",image:"/guitar-neck.jpg",count:"5 modules"},{view:"chords",label:"Chord library",copy:"Shapes, voicings and chord-tone maps.",image:"/guitar-strings.jpg",count:`${CHORDS.length} chords`},{view:"scales",label:"Scale library",copy:"Eleven sounds mapped across the neck.",image:"/guitar-neck.jpg",count:`${SCALES.length} families`},{view:"songs",label:"Songs",copy:"Put chords and rhythm into real music.",image:"/guitar-stage.jpg",count:"6 songs"}];
  return <section className="library-launchpad"><div className="section-head"><h2>Explore the full library</h2><span>Choose a way into the neck</span></div><div>{cards.map((card) => <button onClick={() => go(card.view)} key={card.label} style={{backgroundImage:`linear-gradient(110deg,rgba(35,36,47,.86),rgba(35,36,47,.2)),url(${card.image})`}}><span>{card.count}</span><strong>{card.label}</strong><small>{card.copy}</small><b>Open →</b></button>)}</div></section>;
}

function ChordLibrary({ go, onOpen }: { go:(view:View)=>void; onOpen:(id:string)=>void }) {
  const [quality,setQuality] = useState("All"); const [root,setRoot] = useState("All"); const [level,setLevel] = useState("All levels");
  const visible = CHORDS.filter((chord) => (quality === "All" || chord.quality === quality) && (root === "All" || chord.root === root) && (level === "All levels" || chord.level === level));
  return <div className="screen-content library-screen chord-library-screen"><AppHeader title="Chord library" meta={`${visible.length} shapes`} onBack={() => go("keys")}/><section className="library-photo-hero chord-photo"><div><p className="kicker">Shape meets harmony</p><h2>Every chord is a small map of the key.</h2><p>Choose a shape to see its exact notes, intervals, fingering, movable positions and practice path.</p></div><span>{CHORDS.length} essential shapes</span></section><div className="library-controls"><SegmentTabs labels={["All","Major","Minor","Seventh","Suspended"]} active={quality} onChange={setQuality}/><div className="chip-scroll">{["All levels","Beginner","Intermediate","Advanced"].map((item) => <button className={level === item ? "active" : ""} onClick={() => setLevel(item)} key={item}>{item}</button>)}</div><div className="chip-scroll">{["All","G","C","D","A","E","F","B"].map((key) => <button className={root === key ? "active" : ""} onClick={() => setRoot(key)} key={key}>{key}</button>)}</div></div><div className="chord-library-grid">{visible.map((chord) => <article className="chord-library-card" key={chord.id} style={cssVars(chord.root)}><button className="library-card-open" onClick={() => onOpen(chord.id)}><div><span className="chord-symbol">{chord.symbol}</span><span><strong>{chord.name}</strong><small>{chord.notes}</small></span><b>→</b></div><Fretboard notes={chord.fingering} low={0} high={4} mini rootKey={chord.root}/></button><footer><span className="experience-badge">{chord.level}</span><span>{chord.formula}</span><button className="sample-play" onClick={() => playTones(chord.root,chordIntervals(chord))}>▶ Hear</button></footer></article>)}</div></div>;
}

function ChordDetail({ go, chordId }: { go:(view:View)=>void; chordId:string }) {
  const chord = CHORDS.find((item) => item.id === chordId) ?? CHORDS[0]; const [tab,setTab] = useState("Shape"); const [voicing,setVoicing] = useState("Open");
  const intervals = chordIntervals(chord);
  const range = voicing === "Open" ? [0,4] : voicing === "Barre" ? [2,7] : [5,10]; const displayNotes = voicing === "Open" ? chord.fingering : intervalShape(chord.root,intervals,range[0],range[1]);
  return <div className="screen-content detail-screen chord-detail-screen" style={cssVars(chord.root)}><AppHeader title={chord.name} meta="Chord library" onBack={() => go("chords")}/><section className="chord-detail-hero"><div><span className="chord-big-symbol">{chord.symbol}</span><div><p className="kicker">{chord.quality} chord · {chord.level}</p><h2>{chord.notes}</h2><p>{chord.tip}</p><button className="sample-play detail-audio" onClick={() => playTones(chord.root,intervals)}>▶ Hear chord</button></div></div><div className="chord-hero-photo" aria-label="Close-up guitar strings"/></section><SegmentTabs labels={["Shape","Notes","Practice"]} active={tab} onChange={setTab}/>{tab === "Shape" && <><div className="voicing-picker">{["Open","Barre","Triad"].map((item) => <button className={voicing === item ? "active" : ""} onClick={() => setVoicing(item)} key={item}><strong>{item}</strong><span>{item === "Open" ? "Foundational" : item === "Barre" ? "Movable" : "Strings 1–3"}</span></button>)}</div><div className="fretboard-stage"><div className="section-head"><h2>{voicing} voicing</h2><span>Root notes are square</span></div><Fretboard notes={displayNotes} low={range[0]} high={range[1]} labelMode="note" rootKey={chord.root}/></div><section className="chord-facts"><article><span>Formula</span><strong>{chord.formula}</strong><p>The interval recipe stays the same wherever this chord moves.</p></article><article><span>Chord tones</span><strong>{chord.notes}</strong><p>Find these notes inside nearby scale shapes to build solos around the chord.</p></article><article><span>Best next move</span><strong>{chord.root === "G" ? "C or D" : "Return to G"}</strong><p>Practice one-bar changes without breaking the pulse.</p></article></section></>}{tab === "Notes" && <section className="chord-note-theory"><header><h2>See the chord inside the scale</h2><p>{chord.name} uses {chord.formula.replaceAll(" · ",", ")}. The root gives the chord its name, the third defines major or minor, and the fifth stabilizes the sound.</p></header><div className="theory-visual-grid"><div className="theory-fretboard"><div className="section-head"><h2>Across the guitar</h2><span>Every available voicing</span></div><Fretboard notes={intervalShape(chord.root,intervals,0,12)} low={0} high={12} labelMode="degree" rootKey={chord.root}/><p>The same three or four chord tones repeat across strings. A voicing simply chooses one reachable set.</p></div><PianoMap rootKey={chord.root} intervals={intervals}/></div></section>}{tab === "Practice" && <article className="chord-practice"><div><p className="kicker">Four-minute chord lab</p><h2>Make every note speak</h2><ol><li>Play each string separately and fix muted notes.</li><li>Strum four quarter notes at 72 bpm.</li><li>Change away and return without looking.</li></ol></div><button className="primary-action" onClick={() => go("runner")}>Start practice →</button></article>}</div>;
}

function ScaleLibrary({ go, selectedKey, onOpen }: { go:(view:View)=>void; selectedKey:KeyName; onOpen:(id:string)=>void }) {
  const [family,setFamily] = useState("All"); const [level,setLevel] = useState("All levels"); const visible = SCALES.filter((scale) => (family === "All" || scale.family === family) && (level === "All levels" || scale.level === level));
  return <div className="screen-content library-screen scale-library-screen"><AppHeader title="Scale library" meta={`Key of ${selectedKey}`} onBack={() => go("keys")}/><section className="library-photo-hero scale-photo"><div><p className="kicker">One neck, many sounds</p><h2>Compare scale families without losing your place.</h2><p>Every pattern is generated from the selected key and opens into connected fretboard positions.</p></div><span>{SCALES.length} scales · {selectedKey} active</span></section><div className="library-controls"><SegmentTabs labels={["All","Major","Minor","Pentatonic","Modes"]} active={family} onChange={setFamily}/><div className="chip-scroll">{["All levels","Beginner","Intermediate","Advanced"].map((item) => <button className={level === item ? "active" : ""} onClick={() => setLevel(item)} key={item}>{item}</button>)}</div></div><div className="scale-library-grid">{visible.map((scale,index) => <article className="scale-library-card" key={scale.id} style={cssVars(FIFTHS[(FIFTHS.indexOf(selectedKey)+index)%12])}><button className="library-card-open" onClick={() => onOpen(scale.id)}><div><span>{String(index+1).padStart(2,"0")}</span><strong>{selectedKey} {scale.name}</strong><small>{scale.mood}</small></div><Fretboard notes={intervalShape(selectedKey,scale.intervals,1,7)} low={1} high={7} mini rootKey={selectedKey}/></button><footer><span className="experience-badge">{scale.level}</span><b>{scale.formula}</b><button className="sample-play" onClick={() => playTones(selectedKey,scale.intervals,true)}>▶ Hear</button></footer></article>)}</div></div>;
}

function ScaleLibraryDetail({ go, selectedKey, scaleId }: { go:(view:View)=>void; selectedKey:KeyName; scaleId:string }) {
  const scale = SCALES.find((item) => item.id === scaleId) ?? SCALES[0]; const [tab,setTab] = useState("Fretboard"); const [position,setPosition] = useState(1); const windows = [[0,4],[2,6],[4,8],[6,10],[8,12]]; const [low,high] = windows[position-1]; const keyScale=majorScale(selectedKey); const keyChords=keyScale.map((note,index) => `${note}${["","m","m","","","m","dim"][index]}`);
  return <div className="screen-content detail-screen scale-library-detail" style={cssVars(selectedKey)}><AppHeader title={`${selectedKey} ${scale.name}`} meta="Scale library" onBack={() => go("scales")}/><section className="scale-detail-banner"><div><p className="kicker">{scale.level} · {scale.mood}</p><h2>{scale.formula}</h2><p>{scale.intervals.length} notes · five connected positions · root notes shown as squares</p><button className="sample-play detail-audio" onClick={() => playTones(selectedKey,scale.intervals,true)}>▶ Hear scale</button></div><Ring value={72} size={110} label="72 percent learned"/></section><SegmentTabs labels={["Fretboard","Harmony map","Practice"]} active={tab} onChange={setTab}/>{tab === "Fretboard" && <><div className="position-picker">{[1,2,3,4,5].map((item) => <button className={position === item ? "active" : ""} onClick={() => setPosition(item)} key={item}>Position {item}</button>)}</div><div className="fretboard-stage"><div className="section-head"><h2>Position {position}</h2><span>Frets {low}–{high}</span></div><Fretboard notes={intervalShape(selectedKey,scale.intervals,low,high)} low={low} high={high} labelMode="degree" rootKey={selectedKey}/></div><section className="position-overlap"><div><span>Previous overlap</span><strong>{Math.max(0,low-2)}–{low}</strong></div><i/><div><span>Current window</span><strong>{low}–{high}</strong></div><i/><div><span>Next overlap</span><strong>{high}–{Math.min(12,high+2)}</strong></div></section></>}{tab === "Harmony map" && <><article className="tab-copy"><h2>A scale is the source; chords are selected stacks</h2><p>Build a chord from every {selectedKey} scale degree by taking alternating notes. The shape changes, but every chord tone still comes from the same key map.</p><div className="formula-steps">{scale.intervals.map((interval,index) => <span key={interval}><strong>{index+1}</strong><small>{interval} semitones</small></span>)}</div></article><section className="scale-to-chords"><div className="section-head"><h2>{selectedKey} major harmony</h2><span>Tap a chord to hear it</span></div><div>{keyChords.map((chord,index) => <button onClick={() => playTones(PC_KEY[(keyPc(selectedKey)+[0,2,4,5,7,9,11][index])%12],index === 1 || index === 2 || index === 5 ? [0,3,7] : index === 6 ? [0,3,6] : [0,4,7])} key={chord}><span>{["I","ii","iii","IV","V","vi","vii°"][index]}</span><strong>{chord}</strong><small>{["home","leaves","color","opens","pulls home","relative minor","tension"][index]}</small><b>▶</b></button>)}</div><p>Practice link: target the nearest note from each chord while staying inside the scale position above.</p></section></>}{tab === "Practice" && <article className="chord-practice"><div><p className="kicker">Seven-minute scale-to-chord lab</p><h2>Connect position {position} to harmony</h2><ol><li>Ascend the scale at 72 bpm.</li><li>Play I–IV–V and name each chord tone.</li><li>Improvise, landing on the active chord&apos;s nearest tone.</li></ol></div><button className="primary-action" onClick={() => go("runner")}>Start practice →</button></article>}</div>;
}

function SongLibrary({ go, onOpen }: { go:(view:View)=>void; onOpen:(id:string)=>void }) {
  const [level,setLevel] = useState("All"); const [query,setQuery] = useState(""); const visible = SONGS.filter((song) => (level === "All" || song.level === level) && `${song.title} ${song.artist}`.toLowerCase().includes(query.toLowerCase()));
  return <div className="screen-content library-screen song-library-screen"><AppHeader title="Songs" meta={`${visible.length} arrangements`} onBack={() => go("keys")}/><section className="songs-hero"><div><p className="kicker">Play music sooner</p><h2>Turn the shapes you know into complete songs.</h2><p>Each arrangement connects chord changes, rhythm, tempo and the key map.</p></div><div className="song-search"><label htmlFor="song-search">Find a song</label><input id="song-search" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Title or artist…"/></div></section><SegmentTabs labels={["All","Beginner","Intermediate"]} active={level} onChange={setLevel}/><div className="song-library-grid">{visible.map((song) => <button className="song-card" onClick={() => onOpen(song.id)} key={song.id} style={{...cssVars(song.key),backgroundImage:`linear-gradient(180deg,rgba(25,25,30,.04),rgba(25,25,30,.88)),url(${song.image})`}}><span className="key-chip">Key {song.key}</span><div><small>{song.level} · {song.tempo} bpm</small><strong>{song.title}</strong><p>{song.artist}</p><footer>{song.progression.map((chord) => <span key={chord}>{chord}</span>)}</footer></div></button>)}</div></div>;
}

function SongDetail({ go, songId, onChord }: { go:(view:View)=>void; songId:string; onChord:(id:string)=>void }) {
  const song = SONGS.find((item) => item.id === songId) ?? SONGS[0]; const [section,setSection] = useState("Verse"); const [playing,setPlaying] = useState(false);
  const openChord = (symbol:string) => { const chord = CHORDS.find((item) => item.symbol === symbol); if (chord) onChord(chord.id); };
  return <div className="screen-content detail-screen song-detail-screen" style={cssVars(song.key)}><AppHeader title={song.title} meta={song.artist} onBack={() => go("songs")}/><section className="song-detail-hero" style={{backgroundImage:`linear-gradient(90deg,rgba(20,21,28,.94),rgba(20,21,28,.28)),url(${song.image})`}}><div><span>{song.level} arrangement</span><h2>{song.title}</h2><p>{song.artist}</p><div><b>{song.tempo} bpm</b><b>Key {song.key}</b><b>{song.capo}</b></div></div><button onClick={() => setPlaying(!playing)}>{playing ? "Pause count-in" : "Start count-in"}<span>{playing ? "Ⅱ" : "▶"}</span></button></section><div className="song-workspace"><section><SegmentTabs labels={["Verse","Chorus","Bridge"]} active={section} onChange={setSection}/><div className="song-timeline">{song.progression.concat(song.progression).map((chord,index) => <button onClick={() => openChord(chord)} key={`${chord}${index}`}><span>{index+1}</span><strong>{chord}</strong><small>{index%2 ? "2 beats" : "4 beats"}</small></button>)}</div><p className="song-instruction">{section === "Verse" ? `Keep the ${song.focus.toLowerCase()} steady and let every chord land on beat one.` : section === "Chorus" ? "Open the strum slightly and let the top strings carry the lift." : "Reduce the pattern to down-strokes, then rebuild the groove."}</p></section><aside><div className="section-head"><h2>Chord set</h2><span>Click to study</span></div>{song.progression.map((symbol) => { const chord=CHORDS.find((item)=>item.symbol===symbol); return <button onClick={() => chord && onChord(chord.id)} key={symbol}><span className="relation-key">{symbol}</span><span><strong>{chord?.name ?? symbol}</strong><small>{chord?.notes ?? "Song chord"}</small></span><b>→</b></button>; })}<div className="song-practice-goal"><span>Practice goal</span><strong>3 clean loops</strong><i><b style={{width:"34%"}}/></i></div></aside></div></div>;
}

function Keys({ selectedKey, setSelectedKey, go }: { selectedKey: KeyName; setSelectedKey: (key: KeyName) => void; go: (view: View) => void }) {
  const scale = majorScale(selectedKey); const qualities = ["","m","m","","","m","dim"]; const romans = ["I","ii","iii","IV","V","vi","vii°"];
  const index = FIFTHS.indexOf(selectedKey); const neighbours = [FIFTHS[(index+11)%12],FIFTHS[(index+1)%12]];
  return <div className="screen-content keys-screen"><StatusBar end="Keys"/><CircleOfFifths selectedKey={selectedKey} onSelect={setSelectedKey}/><section className="section chord-section"><div className="section-head"><h2>Its seven chords</h2><button onClick={() => go("key-detail")}>Open key →</button></div><div className="chord-chips">{scale.map((note,index) => <span key={note}><i style={{ background:palette(FIFTHS[(FIFTHS.indexOf(selectedKey)+[0,2,4,-1,1,3,5][index]+12)%12]).bright }}/><small>{romans[index]}</small>{note}{qualities[index]}</span>)}</div><p>Keys next to each other on the wheel share six of seven notes. That overlap is why moving one step around the circle sounds close, not abrupt.</p></section><aside className="desktop-key-relations"><div className="section-head"><h2>Closest harmonic neighbours</h2><span>6 shared notes</span></div><div>{neighbours.map((key) => <button key={key} onClick={() => setSelectedKey(key)} style={cssVars(key)}><span className="relation-key">{key}</span><span><strong>{key} major</strong><small>{majorScale(key).join(" · ")}</small></span><b>→</b></button>)}</div><p>Move clockwise for more forward pull; move counter-clockwise for a softer return.</p></aside><LibraryLaunchpad go={go}/></div>;
}

function Progress({ go, selectedKey }: { go: (view: View) => void; selectedKey:KeyName }) {
  const [range,setRange] = useState<"7 days" | "14 days" | "30 days">("14 days");
  const data = { "7 days":[74,71,79,76,82,84,87], "14 days":[61,68,64,74,71,79,76,82,78,85,83,87,84,87], "30 days":[55,58,57,61,60,64,63,66,68,65,70,69,72,74,71,76,75,79,78,81,80,83,82,85,84,86,85,87,86,87] };
  const values = data[range]; const points = values.map((v,i) => `${i/(values.length-1)*100},${100-(v-55)/40*82}`).join(" ");
  const consistency = Array.from({length:182},(_,index) => { const wave=Math.sin(index*.73)+Math.cos(index*.29)+Math.sin(index*.11); return index%17 === 0 || index%29 === 0 ? 0 : Math.max(0,Math.min(4,Math.round(2+wave))); });
  return <div className="screen-content"><AppHeader title={`${selectedKey} progress`} meta="Consistency" action={<button className="text-action" onClick={() => go("routines")}>Routines</button>}/><div className="desktop-progress-range"><span>Performance window</span><div>{(["7 days","14 days","30 days"] as const).map((item) => <button className={range === item ? "active" : ""} onClick={() => setRange(item)} key={item}>{item}</button>)}</div></div><article className="dial-card"><Ring value={87} size={142}/><div><p className="kicker">Neck knowledge · {selectedKey}</p><h2>87% accurate</h2><p>Your cleanest week yet. You are finding notes before the shape has time to disappear.</p><div className="mini-stats"><span><strong>31</strong> day streak</span><span><strong>1,284</strong> reps</span></div></div></article><section className="chart-card"><div className="section-head"><h2>Daily accuracy</h2><span>Last {range}</span></div><svg viewBox="0 0 100 100" preserveAspectRatio="none" aria-label={`Daily accuracy rising to ${values.at(-1)} percent over ${range}`}><defs><linearGradient id="spark" x1="0" x2="1"><stop stopColor="oklch(.58 .14 262)"/><stop offset="1" stopColor="oklch(.55 .15 148)"/></linearGradient><linearGradient id="area" x1="0" x2="0" y2="1"><stop stopColor="oklch(.82 .07 289)" stopOpacity=".55"/><stop offset="1" stopColor="transparent"/></linearGradient></defs><polygon points={`0,100 ${points} 100,100`} fill="url(#area)"/><polyline points={points} fill="none" stroke="url(#spark)" strokeWidth="2.2" vectorEffect="non-scaling-stroke"/></svg></section><section className="heat-card consistency-card"><div className="section-head"><h2>Practice consistency</h2><span>26 weeks · 94 active days</span></div><div className="commit-months"><span>Mar</span><span>Apr</span><span>May</span><span>Jun</span><span>Jul</span><span>Aug</span></div><div className="commit-layout"><div className="commit-days"><span>Mon</span><span>Wed</span><span>Fri</span></div><div className="commit-graph" aria-label="Practice activity across 26 weeks">{consistency.map((level,index) => <span className={`level-${level}`} key={index} title={level ? `${level*6} minutes practiced` : "No practice"}/>)}</div></div><div className="commit-foot"><span>Daily practice builds the streak</span><div><small>Less</small>{[0,1,2,3,4].map((level) => <i className={`level-${level}`} key={level}/>)}<small>More</small></div></div></section><section className="desktop-progress-insights"><article><span>Fastest gain</span><strong>String 4</strong><small>+18% this month</small><i><b style={{width:"82%"}}/></i></article><article><span>Needs another pass</span><strong>Frets 8–10</strong><small>64% recall</small><i><b style={{width:"64%"}}/></i></article><article><span>Next milestone</span><strong>90% accuracy</strong><small>3 points away</small><i><b style={{width:"87%"}}/></i></article></section></div>;
}

function Routines({ go, selectedKey }: { go: (view: View) => void; selectedKey:KeyName }) {
  return <div className="screen-content routines-screen"><AppHeader title={`${selectedKey} practice routines`} meta="Habit builder"/><p className="routines-intro">Each routine keeps one key active while skills build in a deliberate order.</p><section className="routine-principle"><span>01 Warm hands</span><i>→</i><span>02 Map the key</span><i>→</i><span>03 Target harmony</span><i>→</i><span>04 Make music</span></section><section className="desktop-routine-summary"><div><span>This week</span><strong>3</strong><small>sessions</small></div><div><span>Practice time</span><strong>68</strong><small>minutes</small></div><div><span>Current context</span><strong>{selectedKey}</strong><small>major · all drills</small></div><button onClick={() => go("routine-detail")}>Open today&apos;s plan <span>→</span></button></section><div className="routine-list">{ROUTINES.map((routine,index) => { const total = routine.drills.reduce((sum,d) => sum+d.mins,0); return <article className="routine-card" key={routine.name} style={cssVars(FIFTHS[(FIFTHS.indexOf(selectedKey)+index*2)%12])}><div className="routine-title"><div><span className="key-chip">Key {selectedKey}</span><h2>{routine.name}</h2><p>{total} min · {routine.drills.length} linked drills · {routine.cadence}</p></div></div><div className="routine-sequence">{routine.drills.map((d,i) => <div key={`${d.name}${i}`}><span>{String(i+1).padStart(2,"0")}</span><strong>{d.name}</strong><small>{d.phase} · {d.mins} min</small>{i < routine.drills.length-1 && <i>→</i>}</div>)}</div><div className="routine-foot"><span>{[1,2,3].map((n) => <i className={n <= routine.completed ? "on" : ""} key={n}/>)} {routine.last}</span><button onClick={() => go("routine-detail")}>Start routine</button></div></article>; })}</div></div>;
}

function Runner({ go, sessionKey }: { go: (view: View) => void; sessionKey: KeyName }) {
  const [bpm,setBpm] = useState(84); const [paused,setPaused] = useState(false);
  const notes = scaleShape(sessionKey,1,5);
  return <div className="screen-content flow-screen"><StatusBar end="1 of 4"/><div className="step-bar">{[0,1,2,3].map((n) => <i className={n === 0 ? "done" : n === 1 ? "current" : ""} key={n}/>)}</div><div className="runner-hero"><p className="kicker">Key of {sessionKey}</p><h1>Position one, up and back</h1><p>One note per click. Let the top note turn you around without becoming a pause.</p></div><Fretboard notes={notes} low={1} high={5} labelMode="degree" rootKey={sessionKey}/><div className={`metronome ${paused ? "paused" : ""}`}>{[0,1,2,3].map((n) => <i style={{ animationDelay:`${n*(60/bpm)}s`, animationDuration:`${60/bpm}s` }} key={n}/>)}</div><div className="elapsed"><strong>02:18</strong><span>of 4:00</span></div><div className="transport"><button onClick={() => setBpm((n) => Math.max(40,n-4))}>−4<small>bpm</small></button><button className="pause" onClick={() => setPaused(!paused)}>{paused ? "Resume" : "Pause"}<small>{bpm} bpm</small></button><button onClick={() => setBpm((n) => Math.min(180,n+4))}>+4<small>bpm</small></button></div><button className="finish-link" onClick={() => go("summary")}>Finish session</button></div>;
}

function Summary({ go }: { go: (view: View) => void }) {
  return <div className="screen-content summary-screen"><StatusBar end="Complete"/><article><p className="kicker">Session complete</p><Ring value={92} size={168}/><h1>Strong work</h1><p>You kept the pulse through the shape and found every G on the neck. The turnaround is the one place to slow down tomorrow.</p><div className="accuracy-list">{[["Position one",96],["Thirds",88],["Locate G",92],["Changes",84]].map(([name,value]) => <div key={name}><span>{name}</span><strong>{value}%</strong><i><b style={{ width:`${value}%` }}/></i></div>)}</div></article><div className="action-row"><button className="secondary-action">Share</button><button className="primary-action" onClick={() => go("today")}>Back to today</button></div></div>;
}

function GroupedDrills({ go, selectedKey, onOpen }: { go: (view: View) => void; selectedKey:KeyName; onOpen:(id:string)=>void }) {
  const [tab,setTab] = useState("Skill paths"); const categories = ["Neck knowledge","Scale fluency","Chord tones","Harmony"];
  return <div className="screen-content"><AppHeader title={`${selectedKey} learning paths`} meta={`${DRILLS.length} drills`} onBack={() => go("drills")}/><SegmentTabs labels={["Skill paths","Beginner","Intermediate","Advanced"]} active={tab} onChange={setTab}/><div className="group-list">{categories.map((category,index) => { const items=DRILLS.filter((drill) => drill.category === category && (tab === "Skill paths" || drill.level === tab)).slice(0,4); return items.length ? <section key={category} style={cssVars(FIFTHS[(FIFTHS.indexOf(selectedKey)+index*2)%12])}><div className="group-title"><h2><i/>{category}</h2><span>{items.length} step path</span></div>{items.map((drill) => <button key={drill.id} onClick={() => onOpen(drill.id)}><Fretboard notes={drillNotes(drill,selectedKey)} low={drill.low} high={drill.high} mini rootKey={selectedKey}/><div><strong>{drill.name}</strong><i><b style={{width:`${drill.progress}%`}}/></i></div><small>{drill.minutes} min</small></button>)}</section> : null; })}</div></div>;
}

function PianoMap({ rootKey, intervals = [0,2,4,5,7,9,11] }: { rootKey:KeyName; intervals?:number[] }) {
  const root = keyPc(rootKey); const active = new Set(intervals.map((interval) => (root+interval)%12)); const whites = [{name:"C",pc:0},{name:"D",pc:2},{name:"E",pc:4},{name:"F",pc:5},{name:"G",pc:7},{name:"A",pc:9},{name:"B",pc:11}]; const blacks = [{name:"C#",pc:1,left:14.3},{name:"D#",pc:3,left:28.6},{name:"F#",pc:6,left:57.1},{name:"G#",pc:8,left:71.4},{name:"A#",pc:10,left:85.7}];
  return <div className="piano-card"><div className="section-head"><h2>Same notes on a piano</h2><span>One linear octave</span></div><div className="piano-map" aria-label={`${rootKey} notes on a piano keyboard`}><div className="white-keys">{whites.map((key) => <div className={`${active.has(key.pc) ? "active" : ""} ${key.pc === root ? "root" : ""}`} key={key.name}><span>{key.name}</span></div>)}</div>{blacks.map((key) => <div className={`black-key ${active.has(key.pc) ? "active" : ""} ${key.pc === root ? "root" : ""}`} style={{left:`${key.left}%`}} key={key.name}><span>{key.name}</span></div>)}</div><p>A piano shows pitch once from left to right. Guitar repeats those same pitches across six offset strings, which creates multiple routes to the same note.</p></div>;
}

function TheoryLesson({ rootKey }: { rootKey:KeyName }) {
  const scale = majorScale(rootKey); const degrees = ["1","2","3","4","5","6","7"]; const roles = ["home","motion","major colour","tension","anchor","warmth","leading tone"];
  return <section className="theory-lesson"><header><p className="kicker">Theory you can see and play</p><h2>Why {rootKey} major works</h2><p>The scale is not a shape; it is a distance recipe. The guitar gives that recipe several overlapping physical paths.</p></header><div className="theory-visual-grid"><div className="theory-fretboard"><div className="section-head"><h2>Across the guitar</h2><span>Frets 0–7</span></div><Fretboard notes={scaleShape(rootKey,0,7)} low={0} high={7} labelMode="degree" rootKey={rootKey}/><p>Square roots mark every place the key feels finished. Follow the same degree across strings to see why a box is only one slice of the neck.</p></div><PianoMap rootKey={rootKey}/></div><div className="degree-story">{scale.map((note,index) => <div className={index === 0 || index === 4 || index === 6 ? "focus" : ""} key={note}><span>{degrees[index]}</span><strong>{note}</strong><small>{roles[index]}</small></div>)}</div><section className="interval-explainer"><div><span>Whole</span><strong>2 frets</strong><p>On one string, a whole step skips one fret—like moving two piano keys.</p></div><i>→</i><div><span>Half</span><strong>1 fret</strong><p>A half step is the next fret—like two neighboring piano keys, white or black.</p></div><i>→</i><div><span>Octave</span><strong>12 frets</strong><p>The note name repeats, higher in pitch. On guitar it also appears on nearby strings.</p></div></section><div className="guitar-piano-compare"><article><span>On piano</span><h3>Pitch moves in one direction</h3><p>Each key is one semitone higher than the last, so interval distance is visually obvious.</p></article><article><span>On guitar</span><h3>Pitch moves across and along</h3><p>Moving up a fret raises one semitone; changing strings relocates the same notes into new shapes.</p></article><article><span>What to practise</span><h3>Name the degree before the fret</h3><p>Say “root, third, fifth” as you play. Function transfers between keys more reliably than memorized dot patterns.</p></article></div></section>;
}

function TheoryHub({ go, selectedKey }: { go:(view:View)=>void; selectedKey:KeyName }) {
  const modules = [
    {id:"fretboard",number:"01",title:"Fretboard foundations",level:"Beginner",time:"8 min",copy:"See why notes repeat and learn the octave landmarks."},
    {id:"intervals",number:"02",title:"Intervals",level:"Beginner",time:"10 min",copy:"Measure musical distance in frets and by ear."},
    {id:"chords",number:"03",title:"Chord construction",level:"Intermediate",time:"12 min",copy:"Stack thirds to build triads and seventh chords."},
    {id:"harmony",number:"04",title:"Keys & harmony",level:"Intermediate",time:"14 min",copy:"Connect scale degrees, chords and progressions."},
    {id:"rhythm",number:"05",title:"Rhythm & phrasing",level:"Advanced",time:"11 min",copy:"Turn subdivisions into musical guitar phrases."},
  ];
  const [active,setActive] = useState("fretboard"); const selected = modules.find((item) => item.id === active) ?? modules[0];
  const lessonIntervals = active === "intervals" ? [0,3,4,7,12] : active === "chords" ? [0,4,7,11] : active === "harmony" ? [0,2,4,5,7,9,11] : active === "rhythm" ? [0,7,12] : [0,12];
  return <div className="screen-content theory-hub-screen" style={cssVars(selectedKey)}><AppHeader title="Theory" meta={`Key of ${selectedKey}`} onBack={() => go("keys")}/><section className="theory-hub-hero"><div><p className="kicker">See it. Hear it. Play it.</p><h2>Music theory, anchored to the guitar.</h2><p>Start with the fretboard, compare it with one piano octave, then bring every idea back under your fingers.</p><div><span>5 guided modules</span><span>55 minutes</span><span>3 levels</span></div></div><button onClick={() => playTones(selectedKey,[0,4,7,12],true)}><span>▶</span> Hear {selectedKey} resolve</button></section><div className="theory-course-grid">{modules.map((item,index) => <button className={active === item.id ? "active" : ""} onClick={() => setActive(item.id)} key={item.id} style={cssVars(FIFTHS[(FIFTHS.indexOf(selectedKey)+index*2)%12])}><span>{item.number}</span><div><strong>{item.title}</strong><p>{item.copy}</p><small>{item.level} · {item.time}</small></div><i>→</i></button>)}</div><section className="theory-lab"><header><div><p className="kicker">Active lesson · {selected.level}</p><h2>{selected.title}</h2><p>{selected.copy} Every colored note keeps the same pitch identity across the entire app.</p></div><button className="sample-play" onClick={() => playTones(selectedKey,lessonIntervals,true)}>▶ Hear example</button></header>{active === "rhythm" ? <div className="rhythm-theory"><div className="rhythm-count">{["1","&","2","&","3","&","4","&"].map((beat,index) => <span className={index%2 === 0 ? "downbeat" : ""} key={`${beat}${index}`}><i/>{beat}</span>)}</div><div className="rhythm-cards"><article><span>Quarter notes</span><strong>One event per beat</strong><p>Use steady down-strokes first. The foot and hand should agree.</p></article><article><span>Eighth notes</span><strong>Down · up subdivisions</strong><p>The “&” lives exactly halfway between numbered beats.</p></article><article><span>Phrasing</span><strong>Leave intentional space</strong><p>A rest shapes the line as strongly as a played note.</p></article></div></div> : active === "fretboard" ? <><div className="theory-visual-grid"><div className="theory-fretboard"><div className="section-head"><h2>Octave landmarks</h2><span>Same pitch, new register</span></div><Fretboard notes={intervalShape(selectedKey,[0],0,12)} low={0} high={12} labelMode="note" rootKey={selectedKey}/><p>Move twelve frets on one string for an octave. Across strings, the common shortcut is two strings over and two frets higher, adjusted around the B string.</p></div><PianoMap rootKey={selectedKey} intervals={[0]}/></div></> : <><div className="theory-visual-grid"><div className="theory-fretboard"><div className="section-head"><h2>{selected.title} on guitar</h2><span>Degrees from {selectedKey}</span></div><Fretboard notes={intervalShape(selectedKey,lessonIntervals,0,9)} low={0} high={9} labelMode="degree" rootKey={selectedKey}/><p>Read horizontally for distance on one string and vertically for playable shapes. The pitch colors stay fixed while degree labels change with the key.</p></div><PianoMap rootKey={selectedKey} intervals={lessonIntervals.map((interval) => interval%12)}/></div><div className="theory-concept-row">{lessonIntervals.slice(0,4).map((interval,index) => <article key={`${interval}${index}`}><span>{index === 0 ? "Root" : `${interval} semitones`}</span><strong>{["Home","Color","Structure","Resolution"][index]}</strong><p>{index === 0 ? "The tonal center everything is measured from." : `Move ${interval} fret${interval === 1 ? "" : "s"} on one string to hear this distance exactly.`}</p></article>)}</div></>}</section></div>;
}

function DrillHistory({ rootKey }: { rootKey:KeyName }) {
  const sessions = [{day:"Today",bpm:84,accuracy:94,time:"4:02"},{day:"Tue",bpm:80,accuracy:96,time:"4:18"},{day:"Sun",bpm:76,accuracy:89,time:"4:31"},{day:"Thu",bpm:72,accuracy:86,time:"4:47"}];
  return <section className="drill-history"><header><p className="kicker">Practice history</p><h2>This shape is becoming automatic</h2><p>Tempo is up 12 bpm while accuracy has stayed above 86%.</p></header><div className="history-summary"><article><span>Best clean tempo</span><strong>84 <small>bpm</small></strong><b>+12 this month</b></article><article><span>Average accuracy</span><strong>91<small>%</small></strong><b>4-session average</b></article><article><span>Total repetitions</span><strong>148</strong><b>Across 9 sessions</b></article></div><div className="history-grid"><section><div className="section-head"><h2>Tempo and accuracy</h2><span>Last four sessions</span></div><div className="history-bars">{sessions.map((session) => <div key={session.day}><span>{session.day}</span><i><b style={{width:`${session.accuracy}%`}}/></i><strong>{session.accuracy}%</strong><small>{session.bpm} bpm</small></div>)}</div></section><aside><div className="section-head"><h2>Coach note</h2><span>Key of {rootKey}</span></div><strong>The turnaround is the only unstable moment.</strong><p>Your ascending pass averages 97%. The direction change drops to 84%, so isolate the highest two notes before another full run.</p><div><span>Next target</span><b>88 bpm at 92%+</b></div></aside></div><div className="session-log"><div className="section-head"><h2>Session log</h2><span>Most recent first</span></div>{sessions.map((session,index) => <div key={session.day}><span className="history-check">{index < 2 ? "✓" : "·"}</span><strong>{session.day}</strong><span>{session.time}</span><span>{session.bpm} bpm</span><span>{session.accuracy}% accurate</span></div>)}</div></section>;
}

function DrillDetail({ go, sessionKey, drillId }: { go: (view: View) => void; sessionKey: KeyName; drillId:string }) {
  const [tab,setTab] = useState("Lesson"); const drill = DRILLS.find((item) => item.id === drillId) ?? DRILLS[0]; const [tempo,setTempo] = useState(drill.bpm || 72); const [phase,setPhase] = useState(0); const phases = ["Understand","Play slow","Build time"];
  const scale = majorScale(sessionKey); const harmony = [scale[0],`${scale[1]}m`,`${scale[2]}m`,scale[3],scale[4],`${scale[5]}m`,`${scale[6]}dim`];
  return <div className={`screen-content detail-screen drill-detail-screen tab-${tab.toLowerCase().replace(" ","-")}`}><StatusBar end={`${sessionKey} drill`}/><div className="detail-back"><button onClick={() => go("drills")}>Back to drills</button><span className="key-chip">Everything here: {sessionKey} major</span></div><div className="drill-lesson-header"><div className="detail-title"><div className="drill-card-tags"><span>{drill.category}</span><span>{drill.level}</span><span>{drill.minutes} min</span></div><h1>{drill.name}</h1><p>{drill.reason}</p></div><div className="drill-outcome"><span>Finish line</span><strong>{drill.goal}</strong><small>{drill.cue}</small></div></div><SegmentTabs labels={["Lesson","Theory link","History"]} active={tab} onChange={setTab}/>{tab === "Lesson" && <><div className="lesson-progress">{phases.map((item,index) => <button className={index === phase ? "active" : index < phase ? "done" : ""} onClick={() => setPhase(index)} key={item}><span>{index < phase ? "✓" : index+1}</span><strong>{item}</strong><small>{index === 0 ? "See the target" : index === 1 ? "No metronome pressure" : `Lock at ${tempo} bpm`}</small></button>)}</div><section className="drill-board-stage"><div className="section-head"><h2>{sessionKey} major · full neck context</h2><span>Roots are square · colors identify pitch</span></div><Fretboard notes={drillNotes(drill,sessionKey,true)} low={0} high={12} labelMode={phase === 0 ? "degree" : "note"} rootKey={sessionKey}/><div className="board-legend"><span><i className="root-shape"/> Root / home</span><span><i/> Drill tone</span><button onClick={() => playTones(sessionKey,[...drill.intervals],true)}>▶ Hear the pattern</button></div></section><div className="drill-coach-grid"><section><div className="section-head"><h2>Why this drill</h2><span>Transferable skill</span></div><div className="reason-flow"><article><span>See</span><strong>{drill.category}</strong><small>Recognize the same function in every position.</small></article><i>→</i><article><span>Hear</span><strong>{drill.intervals.length} target tones</strong><small>Connect the shape to its sound.</small></article><i>→</i><article><span>Use</span><strong>{drill.goal}</strong><small>Apply it inside real playing.</small></article></div></section><section className="tempo-section"><div className="section-head"><h2>Tempo ladder</h2><span>{tempo} bpm</span></div><div>{[60,72,84,96,108].map((bpm) => <button className={tempo === bpm ? "active" : ""} onClick={() => setTempo(bpm)} key={bpm}>{bpm}</button>)}</div></section><ol className="instruction-list">{[["Orient",`Find every square ${sessionKey} root before playing.`],["Connect",phase === 0 ? "Say each degree aloud as you trace the path." : `Play evenly at ${tempo} bpm. Keep the hand relaxed.`],["Apply",`${drill.cue} Stop after ${drill.goal.toLowerCase()}.`]].map(([title,body],i) => <li key={title}><span>{i+1}</span><div><strong>{title}</strong><p>{body}</p></div></li>)}</ol></div></>}{tab === "Theory link" && <section className="drill-theory-link"><header><p className="kicker">Scale → chord → music</p><h2>This drill lives inside {sessionKey} harmony</h2><p>The scale supplies the notes. Chords emphasize selected degrees. This drill trains you to see those targets without losing the key.</p></header><div className="theory-visual-grid"><div className="theory-fretboard"><div className="section-head"><h2>{sessionKey} scale and drill tones</h2><span>Full first octave</span></div><Fretboard notes={drillNotes(drill,sessionKey,true)} low={0} high={12} labelMode="degree" rootKey={sessionKey}/></div><PianoMap rootKey={sessionKey} intervals={[...drill.intervals]}/></div><div className="scale-chord-bridge">{harmony.map((chord,index) => <article className={([...drill.intervals] as number[]).includes([0,2,4,5,7,9,11][index]) ? "active" : ""} key={chord}><span>{["I","ii","iii","IV","V","vi","vii°"][index]}</span><strong>{chord}</strong><small>{["home","motion","color","open","pull","relative","resolve"][index]}</small></article>)}</div></section>} {tab === "History" && <DrillHistory rootKey={sessionKey}/>}<div className="sticky-action"><button className="secondary-action" onClick={() => go("routines")}>Add to routine</button><button className="primary-action" onClick={() => go("runner")}>Start {drill.minutes}-minute drill</button></div></div>;
}

function KeyDetail({ go, selectedKey }: { go: (view: View) => void; selectedKey: KeyName }) {
  const [tab,setTab] = useState("Scales"); const scale = majorScale(selectedKey); const roles = ["home","passing","bright","pull","anchor","soft","leaning"];
  return <div className="screen-content key-detail-screen"><StatusBar end="Keys"/><div className="key-detail-hero"><span className="key-badge">{selectedKey}</span><div><h1>{selectedKey} major</h1><p>{selectedKey === "G" ? "One sharp" : `${scale.filter((n) => n.includes("#") || n.includes("b")).length} accidentals`} · relative minor {scale[5]}</p></div></div><SegmentTabs labels={["Scales","Chords","Theory"]} active={tab} onChange={setTab}/>{tab === "Scales" && <><section className="section"><div className="section-head"><h2>The seven notes, and what each one does</h2></div><div className="degree-strip">{scale.map((note,i) => <div className={i === 0 || i === 4 ? "strong" : ""} key={note}><strong>{note}</strong><small>{roles[i]}</small></div>)}</div></section><section className="section shape-section"><div className="section-head"><h2>Five shapes, one scale</h2><button onClick={() => go("scale-detail")}>Open shape →</button></div><div className="shape-row">{[[0,3],[2,5],[4,7],[6,9],[8,12]].map(([low,high],i) => <button className={i === 1 ? "active" : ""} onClick={() => go("scale-detail")} key={i}><Fretboard notes={scaleShape(selectedKey,low,high)} low={low} high={high} mini rootKey={selectedKey}/><span>{["I","II","III","IV","V"][i]}</span></button>)}</div><p>Those five shapes are the same seven notes seen from five places on the neck. Learn where they overlap and the neck stops being twelve separate frets.</p></section></>}{tab === "Chords" && <section className="harmony-lesson"><header><h2>Seven chords come from seven scale degrees</h2><p>Stack every other note of the scale. The available thirds determine whether each chord is major, minor or diminished.</p></header><div>{scale.map((note,i) => <article key={note}><span>{["I","ii","iii","IV","V","vi","vii°"][i]}</span><strong>{note}{["","m","m","","","m","dim"][i]}</strong><small>{["home","departure","colour","open","tension","relative minor","leading"][i]}</small></article>)}</div><PianoMap rootKey={selectedKey}/></section>}{tab === "Theory" && <TheoryLesson rootKey={selectedKey}/>}</div>;
}

function ScaleDetail({ go, selectedKey }: { go: (view: View) => void; selectedKey: KeyName }) {
  const [tab,setTab] = useState("Shapes"); const [position,setPosition] = useState(2); const windows = [[0,3],[2,5],[4,7],[6,9],[8,12]]; const [low,high] = windows[position-1];
  return <div className="screen-content detail-screen"><AppHeader title={`Position ${["I","II","III","IV","V"][position-1]}`} meta={`${selectedKey} major`} onBack={() => go("key-detail")}/><p className="detail-lede">A connected piece of the same {selectedKey} major scale. Use the root notes to orient the shape.</p><SegmentTabs labels={["Shapes","Sound","Theory"]} active={tab} onChange={setTab}/>{tab === "Shapes" && <><div className="position-picker">{[1,2,3,4,5].map((n) => <button className={position === n ? "active" : ""} onClick={() => setPosition(n)} key={n}>{["I","II","III","IV","V"][n-1]}</button>)}</div><Fretboard notes={scaleShape(selectedKey,low,high)} low={low} high={high} labelMode="degree" rootKey={selectedKey}/><section className="steps-section"><div className="section-head"><h2>How the steps fall</h2></div><div className="step-pattern">{["W","W","H","W","W","W","H"].map((step,i) => <span className={step === "H" ? "half" : ""} style={{flex:step === "W" ? 2 : 1}} key={i}>{step}</span>)}</div><p>Wide, wide, narrow, wide, wide, wide, narrow. Those two narrow steps are what make it sound major. Move one and you are in a different scale entirely.</p></section></>}{tab === "Sound" && <article className="sound-lesson"><div><p className="kicker">Train the ear</p><h2>Hear distance from the root</h2><p>Resolve phrases to {selectedKey}. The second wants to move, the fourth leans downward, the fifth feels stable, and the seventh strongly pulls home.</p></div><div className="degree-tension">{["1 home","2 open","3 bright","4 pull","5 anchor","6 warm","7 leading"].map((item,index) => <span className={index === 0 || index === 4 ? "strong" : ""} key={item}>{item}</span>)}</div></article>}{tab === "Theory" && <TheoryLesson rootKey={selectedKey}/>}</div>;
}

function RoutineDetail({ go, sessionKey }: { go: (view: View) => void; sessionKey: KeyName }) {
  const [tab,setTab] = useState("Drills"); const routine = ROUTINES[1]; const total = routine.drills.reduce((sum,d) => sum+d.mins,0);
  return <div className="screen-content detail-screen"><AppHeader title={routine.name} meta="Routine" onBack={() => go("routines")}/><p className="detail-lede">{total} minutes · {routine.drills.length} drills · everything in {sessionKey} major</p><SegmentTabs labels={["Drills","Guided","Settings"]} active={tab} onChange={setTab}/>{tab === "Drills" && <ol className="routine-drills">{routine.drills.map((drill,i) => <li className={i === 1 ? "current" : ""} key={drill.name}><span>{i+1}</span><Fretboard notes={scaleShape(sessionKey,1,4).slice(i,i+6)} low={1} high={4} mini rootKey={sessionKey}/><div><strong>{drill.name}</strong><small>{drill.phase} · {drill.mins} min · key {sessionKey}</small></div><em>{i === 0 ? "done" : i === 1 ? "now" : "next"}</em></li>)}</ol>}{tab === "Guided" && <article className="tab-copy"><h2>Stay in the flow</h2><p>Guided mode keeps time, moves you between drills, and shows what is coming next so your hands never cool down.</p></article>}{tab === "Settings" && <article className="tab-copy"><h2>Routine settings</h2><p>Repeat three times a week. Metronome count-in is on and transitions last ten seconds.</p></article>}<div className="action-row sticky-action"><button className="secondary-action">Edit</button><button className="primary-action" onClick={() => go("guided")}>Start guided</button></div></div>;
}

function Guided({ go, selectedKey }: { go: (view: View) => void; selectedKey:KeyName }) {
  const [paused,setPaused] = useState(false); const [step,setStep] = useState(1); const routine = ROUTINES[1]; const current = routine.drills[step];
  return <div className="screen-content flow-screen guided-screen"><StatusBar end={`${selectedKey} · ${step+1} of 5`}/><div className="step-bar five">{routine.drills.map((_,i) => <i className={i < step ? "done" : i === step ? "current" : ""} key={i}/>)}</div><div className="guided-hero"><p className="kicker">Now playing · key of {selectedKey}</p><h1>{current.name}</h1><Ring value={46} size={158}/><strong>02:18</strong><span>remaining</span></div><div className={`metronome ${paused ? "paused" : ""}`}>{[0,1,2,3].map((n) => <i style={{animationDelay:`${n*.71}s`}} key={n}/>)}</div><section className="up-next"><div className="section-head"><h2>Up next · all in {selectedKey}</h2></div>{routine.drills.slice(step+1).map((drill) => <div key={drill.name} style={cssVars(selectedKey)}><i/><strong>{drill.name}</strong><span>{drill.mins} min</span></div>)}</section><div className="transport guided-actions"><button onClick={() => setStep(Math.min(routine.drills.length-1,step+1))}>Skip<small>next drill</small></button><button className="pause" onClick={() => setPaused(!paused)}>{paused ? "Resume" : "Pause"}<small>84 bpm</small></button></div><button className="finish-link" onClick={() => go("routine-detail")}>End routine</button></div>;
}

export default function FretLabPage() {
  const [view,setView] = useState<View>("today");
  const [selectedKey,setSelectedKey] = useState<KeyName>("G");
  const [selectedChord,setSelectedChord] = useState("g-major");
  const [selectedScale,setSelectedScale] = useState("major");
  const [selectedSong,setSelectedSong] = useState("stand-by-me");
  const [selectedDrill,setSelectedDrill] = useState("position-one");
  const [keyPickerOpen,setKeyPickerOpen] = useState(false);
  useEffect(() => { const stored = window.localStorage.getItem("fretlab-key") as KeyName | null; if (stored && FIFTHS.includes(stored)) setSelectedKey(stored); },[]);
  useEffect(() => { window.localStorage.setItem("fretlab-key",selectedKey); },[selectedKey]);
  const go = (next: View) => { setView(next); window.scrollTo({top:0,behavior:"smooth"}); };
  const primaryView: View = ["today","drills","train","keys","progress"].includes(view) ? view : (view.includes("drill") || view === "grouped" ? "drills" : view === "theory" || view.includes("key") || view.includes("scale") || view.includes("chord") || view.includes("song") ? "keys" : view.includes("routine") || view === "guided" ? "progress" : "today");
  const vars = cssVars(selectedKey);
  return <main className="site-shell" style={vars}>
    <div className="design-caption" aria-hidden="true"><span>FretLab</span><strong>Guitar practice</strong><small>One key-colour system across every practice surface.</small></div>
    <div className={`app-frame view-${view}`}>
      <DesktopTopbar view={view} selectedKey={selectedKey} go={go} openKeyPicker={() => setKeyPickerOpen(true)}/>
      <button className="mobile-key-context" onClick={() => setKeyPickerOpen(true)}><span>Global key</span><strong>{selectedKey} major</strong><b>⌄</b></button>
      {view === "today" && <Today go={go} sessionKey={selectedKey}/>}
      {view === "drills" && <Drills go={go} selectedKey={selectedKey} onOpen={(id) => { setSelectedDrill(id); go("drill-detail"); }}/>}
      {view === "train" && <Train selectedKey={selectedKey} setSelectedKey={setSelectedKey}/>}
      {view === "keys" && <Keys selectedKey={selectedKey} setSelectedKey={setSelectedKey} go={go}/>}
      {view === "theory" && <TheoryHub go={go} selectedKey={selectedKey}/>}
      {view === "chords" && <ChordLibrary go={go} onOpen={(id) => { setSelectedChord(id); go("chord-detail"); }}/>}
      {view === "chord-detail" && <ChordDetail go={go} chordId={selectedChord}/>}
      {view === "scales" && <ScaleLibrary go={go} selectedKey={selectedKey} onOpen={(id) => { setSelectedScale(id); go("scale-library-detail"); }}/>}
      {view === "scale-library-detail" && <ScaleLibraryDetail go={go} selectedKey={selectedKey} scaleId={selectedScale}/>}
      {view === "songs" && <SongLibrary go={go} onOpen={(id) => { setSelectedSong(id); go("song-detail"); }}/>}
      {view === "song-detail" && <SongDetail go={go} songId={selectedSong} onChord={(id) => { setSelectedChord(id); go("chord-detail"); }}/>}
      {view === "progress" && <Progress go={go} selectedKey={selectedKey}/>}
      {view === "routines" && <Routines go={go} selectedKey={selectedKey}/>}
      {view === "runner" && <Runner go={go} sessionKey={selectedKey}/>}
      {view === "summary" && <Summary go={go}/>}
      {view === "grouped" && <GroupedDrills go={go} selectedKey={selectedKey} onOpen={(id) => { setSelectedDrill(id); go("drill-detail"); }}/>}
      {view === "drill-detail" && <DrillDetail go={go} sessionKey={selectedKey} drillId={selectedDrill}/>}
      {view === "key-detail" && <KeyDetail go={go} selectedKey={selectedKey}/>}
      {view === "scale-detail" && <ScaleDetail go={go} selectedKey={selectedKey}/>}
      {view === "routine-detail" && <RoutineDetail go={go} sessionKey={selectedKey}/>}
      {view === "guided" && <Guided go={go} selectedKey={selectedKey}/>}
      <BottomNav active={primaryView} go={go}/>
      {keyPickerOpen && <KeySelectorModal selectedKey={selectedKey} onSelect={setSelectedKey} onClose={() => setKeyPickerOpen(false)}/>}
    </div>
  </main>;
}
